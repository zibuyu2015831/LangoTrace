# 三端页面清单

状态：Accepted

适用阶段：SwiftUI App Shell、MVP 早期页面闭环、三端 UI 审查和后续页面新增。

本文档是语迹 LangoTrace 当前页面覆盖的长期事实源，用于避免后续开发、审查和设计迭代遗漏 iPhone、iPad 或 macOS 页面。它记录当前 App 已有页面、入口、能力状态、主要代码路径和后续审查关注点。

本文档不替代：

- `docs/spec/002-navigation-and-routing.md`：定义导航原则、路由边界和平台 IA。
- `docs/spec/003-ui-design-system.md`：定义视觉、组件、状态和可访问性原则。
- `docs/review/`：记录一次性审查轮次和当时证据。
- `docs/plans/`：记录单项任务执行过程。
- `prototypes/`：三端目标设计基准（2026-06-11 重建，入口 `prototypes/index.html`），表达目标设计而非实现事实；标注「目标设计」的页面以本文档状态为实现判断依据。

后续新增、删除、合并或改名页面时，必须同步更新本文档。

## 1. 状态定义

- `Implemented`：已有可交互页面或平台承载，页面可作为当前体验的一部分使用。
- `Local Mock`：使用本地模拟数据或本地状态展示真实级页面，不触发真实外部能力。
- `Unavailable`：能力尚未接入，页面只解释当前边界、后续接入条件和不会发生的副作用。
- `Shell`：外壳或结构页面已存在，但核心业务闭环仍依赖子页面或模拟数据。
- `Planned`：当前没有真实页面或入口，只在路线中存在。本文档不应大量记录 Planned 页面，除非它会影响当前 IA。

## 2. Root 与共享启动页面

| 页面 | 平台 | 用户目的 | 入口 | 当前状态 | 主要代码路径 | 能力边界 | 审查关注点 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| App Root phase | iPhone / iPad / macOS | 根据启动状态进入 Welcome、Onboarding 或 Main | `LangoTraceRootView` 的 `phase` 和 `LaunchRoute` | Shell | `Packages/LangoTraceUI/Sources/LangoTraceUI/LangoTraceRootView.swift` | 缺少语言空间时强制回到 onboarding，不直接创建数据 | 首次启动恢复、语言空间持久化落地后必须复查 |
| Welcome | iPhone / iPad / macOS | 首次解释产品定位并进入设置流程 | App 初次启动或 `phase == .welcome` | Implemented | `WelcomeView.swift`、`WelcomeView+Layout.swift`、`WelcomeTracePreviewCarousel.swift`、`WelcomeCapsuleLabel.swift` | 使用静态示例和本地 UI，不创建语言空间 | 首屏不能营销化；三端布局不能压缩、溢出或遮挡 |
| Onboarding | iPhone / iPad / macOS | 选择母语、目标语言和水平，创建第一个语言空间 | Welcome 完成后或缺少语言空间 | Implemented | `OnboardingView.swift`、`LangoTraceRootView.swift`、`LangoTraceApp/AppEnvironment.swift` | 通过 App Shell action 调用 `LanguageSpaceRepository` 创建真实语言空间；当前默认装配 SQLite / GRDB 语言空间 repository 并支持启动恢复。compact 宽度使用底部 sticky CTA，iPad / macOS 宽屏使用内容流内 CTA 和下移的页面 stage；创建按钮按主操作宽度居中约束，不铺满宽屏 | 后续应继续检查创建失败恢复、删除最后空间回到 onboarding、动态字体和无障碍 |
| Platform Main 分发 | iPhone / iPad / macOS | 按设备进入专属主界面 | `phase == .main` 且存在语言空间 | Shell | `LangoTraceRootView.swift` | iOS 通过 `UIDevice.current.userInterfaceIdiom` 区分 iPhone / iPad；macOS 走 Mac 主界面 | 分平台 UI 不应退化为同一套放大布局 |

## 3. iPhone 页面清单

iPhone 的顶层结构是 `记录 / 阅读 / 练习 / 记忆` 四个 Tab，设置通过顶部 gear 和二级 route 稳定可达。当前代码事实源是 `PhoneMainView.swift`、`PhoneMainSections.swift`、`PhoneMainSupportingViews.swift`、`PhotoWritingView.swift`、`ReadingViews.swift`、`LearningContentComponents.swift`、`SentencePairActionControls.swift` 和 `PhonePracticeRows.swift`。

| 页面 | 用户目的 | 入口 | 当前状态 | 主要代码路径 | 能力边界 | 审查关注点 |
| --- | --- | --- | --- | --- | --- | --- |
| iPhone Main Shell | 承载四 Tab、NavigationStack、sheet 和语言空间上下文 | Root 进入 iPhone main | Shell | `PhoneMainView.swift` | `contentStore.ensureSeeded()` 使用本地种子数据；Reading Tab 使用 `ReadingLibraryStore` 加载当前语言空间资料库 | Tab 数量保持克制；sheet 不应抢占主流程 |
| 记录 Tab | 随手创建生活记录，查看最近记录 | 底部 `记录` Tab | Implemented | `PhoneRecordWorkspaceView` in `PhoneMainSections.swift` | 最近记录来自本地 repository / mock seed；日期分组 + 三枚筛选 chip（全部、照片写作、待练习；已记忆 chip 待 E7 显示）；无记录时展示空记录状态，筛选无结果时展示筛选空状态 | 首屏应突出写一句和照片写作，避免说明卡堆叠 |
| 记录 Hero | 提供高频记录入口 | 记录 Tab 顶部 | Implemented | `HeroActionCard` in `PhoneMainSupportingViews.swift` | `写一句` 打开文本编辑；`用照片开始` 打开本地照片写作预览 | 不恢复旧的 `听` 首屏入口；按钮文案不能溢出 |
| 文本记录编辑 | 输入标题和正文，保存为本地记录 | `写一句`、iPad 新建、部分 Mac 复用路径 | Implemented | `EntryEditorView` in `PhoneMainSupportingViews.swift` | iOS 主路径保存到 GRDB learning content repository；保存动作不触发 AI、同步或导出 | 隐私说明保持短；iPad / Mac 创建入口仍需在后续平台接入时复查 async 保存和失败恢复 |
| 照片写作 | 从相册选取照片，剥离 EXIF GPS 元数据，生成缩略图，保存为照片写作记录并进入详情；显式触发 AI 看图辅助写作（写作提示 / 母语草稿） | 记录 Hero 的 `用照片开始` | Implemented | `PhotoWritingView.swift`、`PhotoWritingSaveCoordinator.swift`、`PhotoWritingActions.swift`、`PhotoWritingAssistViewModel.swift`、`PhotoWritingAssistResultPanel.swift`、`PhotoImportPipeline`、`AIImageSanitizer`、`PhotoWritingAssistService`、`GRDBEntryPhotoAttachmentRepository` | 通过 `PhotosUI.PHPickerViewController` 从本地相册选取一张照片；App 在保存前剥离 EXIF GPS 元数据并生成缩略图；**保存路径的照片字节不上传、不同步**，原始照片为用户主资产（`delete_after = NULL`），缩略图为可重建派生资产；只有 Entry 创建和照片附件导入都成功后才跳转到记录详情，导入失败会停留在照片写作页并回滚已创建 Entry。**新增显式「让 AI 看图帮我写」动作**：默认不发送照片，仅在用户点按该动作并经发送前确认后，才把一张经 `AIImageSanitizer` 降采样 + 再次剥离 EXIF/GPS 的脱敏图片 + 可选备注发送给已配置 AI Provider（仅 OpenAI 兼容 Chat / Responses，需用户已启用图片输入），产出写作提示或母语草稿；结果非破坏性（母语草稿可「采用」追加，写作提示只读）；经 `ai_request_logs` 记非敏感日志，capability=`photoWritingAssist` 是唯一在请求预览中含 `photoAttachments` 的能力。页面不再常驻底部隐私提示横幅（已移除以保持简洁），隐私披露收敛到发送前确认弹窗这一唯一界面（披露「将发送照片 + 备注」并与发送动作原子绑定） | 不做 OCR、不做通用图片问答；母语草稿质量依赖 Prompt 调优；不恢复页内常驻隐私提示横幅；专用 `photo_writing_assist_operations` 摘要表、Anthropic/Gemini/mimo 图片、多张照片、照片编辑和照片删除另开方案 |
| 最近记录列表 | 浏览本地生活记录并进入详情 | 记录 Tab 下方列表 | Implemented | `EntryCard` in `PhoneMainSupportingViews.swift` | 展示 seed 和用户本地创建记录；材料状态通过 `EntryMaterialStatusPill`（noMaterial / stale / fresh）展示；`photoWriting` 类型记录在正文右侧异步展示 44×44 缩略图（`PhotoDisplayActions` environment 加载，非 photoWriting 跳过） | 卡片密度、目标语言内容和材料状态 pill 需保持清晰；缩略图异步加载，初次显示前空位不预留空间 |
| 空记录状态 | 没有记录时给出创建入口 | 记录 Tab 无 entries | Implemented | `EmptyEntryPanel` in `PhoneMainSupportingViews.swift` | 只引导创建，不写入数据 | 空状态不能像错误或开发提示 |
| 阅读 Tab | 管理当前语言空间阅读资料并进入阅读 | 底部 `阅读` Tab | Implemented / Reading CRUD / AI / TTS / UITextView selection | `ReadingLibraryView`、`ReadingDocumentDetailView`、`ReadingLibraryStore`、`ReadingDocumentStore`、`ReadingSelectableTextView`、`ReadingMarkdownBlockRenderer`、`ReadingSelectionExplanationService` | iPhone 首屏改为资料库首页：页面顶部与其余三个主 Tab 共用 `phoneRootContextToolbar`（inline 标题 + 语言胶囊 + 设置齿轮，阅读 Tab 不再缺设置入口）；导入主动作、搜索、筛选、文档列表和删除恢复；空库时使用单一空状态卡承接 `粘贴文本` / `导入文件`。点开文档后进入独立阅读详情，详情内显示 Markdown / 纯文本阅读；右上角提供 `编辑` 入口，进入受控 editor sheet，阅读态不再在正文顶部重复显示 metadata block；保存后通过同一 Reading store / repository seam 更新标题与正文、刷新搜索索引与阅读 presentation、并失效旧 selection / explanation / TTS request。阅读 Canvas 使用 `UITextView`（iOS/iPadOS）或 `NSTextView`（macOS）每 Block 渲染连续段落文本，无 Button 行分隔感，支持 Apple 原生文本选择手势（双击=单词，长按拖选=任意片段，三击=整句），禁止"点击句子触发面板"交互；选择 ≥2 个非空白字符后 150ms debounce 触发底部 compact 学习面板（iPhone）或更新侧边 inspector（iPad/macOS）；选择建立后已提交文字有持久高亮标注（浅黄）。compact 面板当前显式区分 `hidden / collapsed / loading / content / failed`，新选中文本先进入 `collapsed`，不会直接展开旧 explanation 结果；面板出现和消失有 `easeInOut(0.22s)` 动画。iPad：侧边 inspector 可用时选择直接更新 inspector；inspector 可折叠（顶部按钮），折叠后降级为画布底部 compact 面板，`选中 → 解释` 路径保持可用，折叠 / 展开不触发 AI 或 TTS。macOS：侧边 inspector 常驻，右键菜单注入"用语迹解释"；不展示底部 compact 面板。解释请求按全文长度动态选择上下文；点击 `听` 进入 TTS coordinator；不自动发送全文、不自动 TTS、不做 EPUB/PDF/HTML。资料库行展示 `字数·导入日期·进度` 三段元信息，含收藏星标；筛选区提供 `全部 / 收藏` chips；解释面板（compact 面板与 iPad / macOS inspector）加入三档解释语言模式切换（母语 / 双语 / 沉浸），切换后对当前选区重发请求，缓存命中时不重发 | 后续：UITextView 高度计算在初始化时可能需要两帧稳定；跨 block 文本选择不支持（per-block 粒度）；全文档单一 TextKit 视图为长期优化；阅读位置滚动采集与恢复、词典 UI、长文滚动和 VoiceOver 仍需人工验收 |
| 记录详情 | 阅读和编辑原始记录、生成学习材料、编辑 learning text、重新分析和逐句进入练习 | 最近记录、保存后、照片写作创建后 | Implemented | `EntryDetailView`、`EntryDetailHeader.swift`、`EntryDetailPhotoPresentation.swift`、`SentencePairView`、`LearningMaterialGenerationActions` | iPhone / iPad / macOS 均通过共享 `EntryDetailView` 接入真实 `生成学习材料` action；`photoWriting` 类型记录在原文卡片上方异步展示照片卡片（`PhotoDisplayActions` environment 加载），成功态使用缩放适配、背景、圆角、描边和高度上限，加载中、附件缺失或图片解码失败时显示同一外层 chrome 的可见照片状态区域；iPhone compact 入口使用当前记录标题作为 navigation title，正文不再重复记录标题，长标题遵循系统单行截断，空白标题 fallback 到 `记录详情`；iPad / macOS 入口显式选择嵌入式标题策略，继续保留大屏工作台语义；记录详情不展示来源 / 语言 / 场景 metadata 或 `可用` 等能力状态 badge；无 material 时只有一个核心 AI 动作，点击后经 App Shell 发送当前文本给已配置 AI Provider 并保存 GRDB LearningMaterial；原始 Entry 正文和 learning text 均使用动态文本卡片与 sheet 编辑，超长文本按行数折叠（`EntrySourceCollapsePresentation`，`lineLimit` + 展开/收起，尊重 Dynamic Type，不固定像素高度）；英文表达卡的「重新翻译（按中文记录）/ 重新讲解（当前英文）」收敛为常驻「重新生成」`Menu` + 覆盖 `confirmationDialog`（`LearningMaterialActionAvailability` 决策，不再按 stale 条件显隐，仅运行中 / blocked 态禁用）；英文表达卡新增 `阅读全文`（book）入口进入双语沉浸阅读页；原文编辑只保存本地 Entry，不自动外发，已有 material 会显示”基于旧记录”并提供显式重新生成；learning text 编辑后可 `重新分析`；记录详情不再在句子列表后追加 practice candidate 信息卡，练习入口收敛到每句 `练` 和练习 Tab / 平台练习列表 | 不恢复标题区 metadata 或能力状态 badge；不恢复本地预览主按钮；不恢复”进入练习”候选信息区；不使用自定义多行 navigation title 或 principal toolbar 承载长标题；生成动作必须披露 AI Provider 边界；照片不发送 AI Provider；历史无附件 photoWriting 记录只显示降级态，不做补图或数据清理；iPad / macOS 仍需后续人工验收大屏布局 |
| 双语沉浸阅读页 | 通读当前记录的目标语言全文，点句显隐母语译文与讲解，可调字号，可连续 TTS 朗读整篇 | 记录详情英文表达卡 `阅读全文` | Implemented / Reading / TTS Sequence | `EntryReadingViews.swift`（`EntryReadingStoreView` / `EntryReadingView`）、`EntryReadingRouting.swift`、`SentenceSequencePlayback`（Core）、`LearningContentStore`、`PhoneRoute.bilingualReading` / `PadWorkspaceRoute.bilingualReading` / `MacWorkspaceRoute.bilingualReading` | 三端共享 `bilingualReading(entryID)` 路由（只带 entryID，不快照 rendering），`EntryReadingStoreView` 以实时 `contentStore.rendering(for:)` 为 source of truth，详情页编辑/重生成后返回不陈旧；逐句排版目标语言，点句切换显隐母语译文 + 讲解（`EntryReadingRevealState`）；句子分析缺失/陈旧时回退整段 `targetText` 不空屏；字号档位用 `@AppStorage`（`EntryReadingPreferenceKey.fontScale`，纯展示性本地 UI 偏好，不入主存储 / 不同步 / 丢失可接受）；逐句 `听` 复用单句 TTS coordinator；连续播放（play.fill / stop.fill）由 store 编排 `playSentenceSequence` / `stopSentenceSequence` 驱动，store 持续播意图、仅在当前句自然播完时自动推进，单句 tap / stop / 离开页先清意图，当前句 accent 描边高亮；进页 `.task` 调 `stopSentenceSequence()` 复位，避免与详情页共享播放态串扰 | 不并入文档库 `阅读` Tab（不同数据通道）；离开页面只停播放不取消正在进行的 TTS 生成（已显式触发的请求自然跑完，符合核心决策 #10）；真机逐句连播体验、duration-fallback 时序与三端布局仍需人工验收 |
| 听一句原位播放 | 针对单句进行 TTS 音频生成、缓存命中播放和原位状态反馈 | 记录详情每句的 `听` | Implemented / TTS Playback | `SentencePairView`、`SentencePairActionControls.swift`、`SentenceAudioPlaybackActions`、`LearningContentStore`、`SentenceAudioPlaybackCoordinator` | 播放按钮不弹解释型 sheet；点击后经 App 注入的 action contract 进入 coordinator，优先命中本地 TTS audio artifact，miss 时只发送用户点击的单句目标语言文本给已测试可用 TTS Provider，生成后写入本地媒体派生资产并自动播放；播放状态在句子卡片原位切换 `listen` / `pause` / generating icon | 不得回退到 `LocalListeningPreviewView` 或任何请求确认 sheet；页面展示、滚动、进入详情、生成学习材料完成和切换句子不得自动触发 TTS；后台播放、批量预生成、录音、跟读评分和费用预算仍需独立方案 |
| 逐句练习入口 | 从单句进入完整练习会话 | 记录详情每句的 `练` | Implemented / Practice Recording | `SentencePairView`、`PracticeRouting.swift`、`PracticeSessionView` | 入口携带 `PracticeSessionRouteSeed`，包含 entry、material、sentence identity、sentence index、target text hash、target language code、exercise type、句子快照和同一篇记录内的轻量 sibling context；记录详情逐句 `练` 当前默认进入 shadowing，听写 / 回译经练习 Tab 句子列表的方式切换进入；单句页只读取已有 LearningMaterial 翻译 / note / 句分析，不新增 AI 语法分析请求 | `听` 只负责 TTS 示范，`练` 负责进入会话；后续词典、单词本、评分仍需独立方案 |
| 练习 Tab | 从生活记录进入句子练习队列 | 底部 `练习` Tab | Implemented / Practice Recording / Mode Foundation | `PracticeView` in `PhoneMainSections.swift`、`PhonePracticeRows.swift`、`PracticeSentenceListView`、`PracticeSentenceListPresentation.swift` | 首层改为可练习记录卡片列表，点击记录进入句子练习列表；不展示“从生活进入练习”标题，不混排听写 / 回译等未完成任务类型；没有 LearningMaterial 的记录不自动触发 AI 生成；句子列表已具备 `PracticeModeAvailability`、已练状态和“从第 n 句继续”CTA，App 注册 `shadowing / dictation / backtranslation` 三种方式，句子列表展示 跟读 / 听写 / 回译 分段控制；“已练”按当前 exercise type 查询 `completedSentenceIDs(materialID:exerciseType:)`（听写 / 回译由文本 attempt 派生，跟读由 ready recording 派生） | 记录卡片需要稳定高度、line limit 和尾部省略；不能把未来练习类型写成当前可用；新练习方式不得只点亮 segment，必须先完成对应真实会话能力 |
| 练习会话（跟读） | 听示范、录音、回放录音、可重复重录，并在同一篇记录内切换上一句 / 下一句 | 练习 Tab 句子列表、记录详情 `练` | Implemented / Practice Recording / Mode Foundation | `PracticeSessionView`、`PracticeShadowingSessionView`、`PracticeShadowingLayout.swift`、`PracticePromptCard.swift`、`PracticeControlBar.swift`、`PracticeSessionViewModel.swift`、`PracticeActions.swift`、`LearningContentStore` | 通过 App 注入 `PracticeActions` 创建 / 恢复单句 session；`PracticeSessionView` 按 route seed 的 exercise type 分发：`dictation` 进入听写会话、`backtranslation` 进入回译会话（均见下行），`shadowing` 进入本行的跟读录音会话；`PracticeSessionRouteSeed` 与 `PracticeSentenceSnapshot` 携带 exercise type，底部句间导航继承当前 mode；单句页采用「句子舞台 + 底部停靠控制台」布局（三端共享）：单一 `ScrollView` 作主滚动体（不被外层 ScrollView 包裹，满足 spec 013）+ `safeAreaInset(.bottom)` 停靠控制台，控制台高度经 `PracticeControlDeckHeightKey` 实测、舞台预留剩余视口高度；舞台把目标句升为居中 Hero（`PracticeShadowingLayout.resolve` 决策：有 session 且无失败时垂直居中、失败态顶部对齐保证提示可见、无 session 时不渲染控制台），控制台内操作条在前、句间导航在后（控制条直接落 deck 表面、已去内层 `langoPanel`，无卡片套卡片描边；deck 仅以 elevatedPaper 背景 + 顶部 hairline 与舞台分隔，控制条保留无描边 `.padding(.vertical, 14)` 竖向呼吸），舞台与控制台限宽 560 居中；只保留内容卡、操作卡、底部句间导航和真正异常状态，不再显示重复 header、不可点击阶段 pill、常驻指导文案、缺少录音提示卡或底部 step card；顶部内容卡采用目标句常驻、`查看释义` 与 `查看讲解` 优先同行、中文释义按需完整展示且展开后不重复标题、讲解按段落留出间距的结构，隐藏释义或讲解时不预留大块空白；prompt card disclosure state 只是 route-local UI state，不进入 `PracticeSession`、recording metadata、TTS cache 或 media artifact；底部句间导航在当前 route 内替换相邻 seed，左侧 `上一句`、中间 `第 n / m 句`、右侧 `下一句`，录音中和录音回放中禁用切换，示范播放中切换会先停止旧播放；单句页听示范复用逐句 TTS playback coordinator；用户显式点击后请求麦克风并录音，开始录音前停止当前示范播放；文件写入 App 管理媒体资产目录和 GRDB practice recording metadata；单句页可回放最近 ready recording，重复录音后回放使用最近一次 ready recording，已有录音时主按钮显示 `再录一次` 并继续允许录音；“已练过”由 ready recording 派生，不要求手动标记完成；录音默认本地、不同步、不默认导出、不自动发送 AI Provider | 第一阶段不做发音评分、ASR、后台录音或录音上传；真实设备麦克风、回放音频路由和三端布局仍需人工验收 |
| 练习会话（听写） | 听该句已有 TTS，输入听到的句子，提交后本机对照参考句并标出差异，可句间导航与再试一次 | 练习 Tab 句子列表选「听写」方式后逐句进入 | Implemented / Practice Dictation | `PracticeSessionView` 分发 → `PracticeDictationSessionView`、`PracticeDictationSessionViewModel.swift`、`PracticeDictationDiff`（Core）、`PracticeActions.submitDictationAttempt`、`GRDBPracticeRepository.recordTextAttempt`、`practice_text_attempts`（v23） | 听写状态机为 `listening`（参考句隐藏：重听 + 已听 n 次 + 输入框 + 提交）→ `compared`（你的听写 + 参考句 + “n 处差异 · 本机对照，不发送 AI” + 再试一次）；对照前参考句不进入可展示视图状态，避免边听边看答案；重听不设限，已听计数为低权重信息；对照是 `PracticeDictationDiff` Core 纯函数（NFC 归一化、Character 分词、词级 LCS 大小写折叠、2000 上限），声学/词汇层差异计入、大小写与标点不计入，完全本机、不调用 AI；差异在用户作答上用下划线（形状）+ warn 色 + 计数文字共同表达；提交先算 diff 再持久化 attempt，持久化失败保留对照结果可重试；句间切换先停止示范播放；attempt 文本为本地主数据，不同步 / 不默认导出 / 不入诊断；“已练”由未软删文本 attempt 派生 | 不做发音评分 / ASR / 错词本；不为听写新增 TTS 请求或批量预生成；不提供严格模式开关；下划线视觉与真机键盘交互、三端布局仍需人工验收 |
| 练习会话（回译） | 读母语原意，写下自己的目标语言表达，点「对照参考」展开参考表达与既有说明，不判对错，可句间导航与再写一次 | 练习 Tab 句子列表选「回译」方式后逐句进入 | Implemented / Practice Backtranslation (Slice 1) | `PracticeSessionView` 分发 → `PracticeBacktranslationSessionView`、`PracticeBacktranslationSessionViewModel.swift`、`PracticeActions.submitBacktranslationAttempt` / `fetchSentenceAnalysis`、`LearningContentRepository.sentenceAnalysis`、`GRDBPracticeRepository.recordTextAttempt`、`practice_text_attempts`（v23） | 回译状态机为 `answering`（题面=母语原意 `translationSnapshot`、输入框、作答非空后可「对照参考」）→ `revealed`（你的表达 + 参考表达 + 自然 / 直译 / 说明 / 重点 + “参考表达不止一种，意思贴近即可” + 再写一次）；参考卡在用户点「对照参考」前隐藏，不判对错、无差异计数、无评分；参考富字段经新增只读 seam `sentenceAnalysis(materialID:sentenceIndex:)` 在 reveal 时懒加载（不预载进 snapshot），seam 缺失或读失败时降级为 `targetTextSnapshot` + `noteSnapshot` 兜底；reveal 同时把作答写入 `practice_text_attempts`（exercise_type=backtranslation，diff 列由 repository 不变量恒 NULL、listen_count 恒 0），持久化与 seam 解耦，seam 失败不影响落表；无母语原意题面的句显示引导态（去记录详情生成学习材料），不内嵌生成请求；全程零网络；attempt 为本地主数据，不同步 / 不默认导出 / 不入诊断；“已练”由未软删文本 attempt 派生 | Slice 1 纯本地，不发任何 AI 请求；可选 AI 点评（Slice 2）双重门禁待 E6 + 单独隐私授权，未实现；不做相似度打分 / 对错判定；参考说明文案不得硬编码，必须来自该句真实分析；三端布局与键盘交互仍需人工验收 |
| 记忆 Tab | 查看从生活记录中沉淀的短语和句子 | 底部 `记忆` Tab | Local Mock | `MemoryView` in `PhoneMainSections.swift` | iPhone 当前隐藏技术性三层记忆基础设施 | 不展示向量索引、embedding 等工程概念 |
| iPhone 设置列表 | 查看语言空间、界面语言、外观、AI、同步、本地数据、隐私、导入导出等能力边界 | 顶部 gear | Implemented | `SettingsView` in `PhoneMainSections.swift` | 设置项多数为能力边界；AI Provider 已接入本地配置保存和 Keychain；界面语言和外观可切换设备级 preference；导入导出只说明能力边界，不打开文件面板；设置主列表显式隐藏通用 `CapabilityStatusBadge`，不把开发阶段角标作为用户信息展示；**E12 起行尾显示真实当前值**（界面语言 / 外观 / AI Provider 已配置·缺密钥·未配置 / 同步未启用 / 本地数据体积），由 `SettingsStatusProjection`（`LearningContentStore.settingsStatus`）投影驱动，渲染零 Keychain / 零网络，本地数据体积后台计算 + 缓存 | 设置不能成为一级 Tab；文案要面向用户而非工程排期；真实设置当前值已通过独立 `SettingsStatusProjection` 实现（E12），不复用开发 completion marker |
| 外观设置页 | 切换跟随系统、浅色或深色外观 | 设置列表 `外观` row | Implemented / Visual QA Pending | `SettingsCapabilityDetailView.swift`、`LangoTraceApp.swift`、`LangoTraceDesign.swift` | 偏好写入设备本地 `UserDefaults`，通过 App 层 `preferredColorScheme` 即时影响当前界面；不进入语言空间、SQLite / GRDB、Keychain、AI Provider、同步或学习数据；light / dark color token 已接入，发布级视觉仍需截图或人工验收 | 选中态不能只靠颜色表达；无当前语言空间时仍应可进入；未来多品牌主题、字体和交互样式不得直接塞入当前三选一设置 |
| AI Provider 设置页 | 配置文本模型、语音生成模型、向量模型，保存本地安全配置并执行分能力配置测试 | 设置列表 `AI Provider` row | Implemented / Configuration Synthetic Probe / TTS / Embedding Probe | `AIProviderSettingsView.swift`、`AIProviderDraftConfiguration.swift`、`AIProviderSettingsModels.swift`、`AIProviderSettingsComponents.swift`、`AIProviderSettingsActions.swift`、`SettingsCapabilityDetailView.swift` | AI Provider 配置是跨语言空间的系统级设置，详情页只保留导航标题，内容区不重复显示 `AI Provider` 或当前语言空间方向 / 等级；主路径按模型用途分组；Provider 行内展示；API Key 有明确字段名和小眼睛显示/隐藏按钮；未保存时 placeholder 显示 `当前未保存 API Key`，已有本机 credential 但未显式查看时显示 `已保存到本机 Keychain`；加载已保存配置只读取非敏感 profile / endpoint / credential metadata，不解析 Keychain 明文；用户点击小眼睛后才通过服务边界解析 Keychain 并回填同一可编辑输入框；文本模型必填，图片理解默认关闭；语音和向量 endpoint 可引用文本模型 API Key 或使用独立 API Key；iPad / macOS 工作台详情复用同一表单，但通过平台最大宽度保持大屏阅读栏；保存动作把非敏感配置写入 SQLite / GRDB，把 API Key 写入 Keychain；语音生成配置以“语音细节”子区显示音色、输出格式、语速和朗读风格，音色 / 格式使用带标题与说明的菜单行，语速使用统一的减 / 数值 / 加控件，“朗读风格”用于可选填写语气、节奏或朗读方式；这些字段按当前语言空间保存 language code 级 voice profile；加载已保存配置时按当前语言空间读取已保存 TTS voice profile，回填 voice、format、speed 和 instructions，避免重开设置页后被 provider 默认值覆盖；测试按钮打开分能力结果面板，当前真实请求覆盖文本回复、JSON 输出、当前语言空间上下文下的语言支持、用户显式启用图片输入后的内置图片理解 probe，以及启用且配置完整时的 OpenAI / OpenRouter TTS 固定低敏测试句 probe；向量化第一阶段在 OpenAI / OpenRouter / Custom OpenAI-compatible 且配置完整时发送固定低敏 embeddings probe，只验证返回向量 shape，不保存 vector；TTS 结果携带 `.tts` endpoint metadata，失败不污染文本 profile 全局最近验证摘要，并在 voice profile 保留最近失败分类供可用性读取；成功结果的样例音频只通过 Speech preview store / playback seam 试听，SwiftUI 不接触 audio bytes 或文件路径；语言支持使用稳定 target language code 由 AI 层 allowlist 派生 Prompt 名称和离线校验规则，不发送目标语言正文或用户内容；iPhone / iPad / macOS 均通过共享 `SettingsCapabilityDetailView` 注入当前语言空间 target language code 作为测试上下文，不复制平台专属 AI Provider 表单；图片理解使用 App 内置白底蓝色正方形 PNG，不使用用户照片或生活记录附件；向量化不发送生活记录、历史记忆、目标语言正文或 Prompt Preset 内容；iPhone compact 使用 bottom sheet detents，结果面板隐藏系统 drag indicator，测试中采用居中 `ProgressView` 状态 chrome，完成后顶部回到中性 `测试请求` 标题，能力结果使用 grouped capability card 和细分隔；成功态隐藏底部 prominent `重新测试` 按钮，非成功完成态保留重试恢复操作；iPad 常规宽度和 macOS 不强制套用移动端 detents，结果内容在大屏使用固定最大宽度避免横向拉满；测试请求不发送生活记录、用户照片、历史记忆、目标语言正文或 Prompt Preset 内容 | 真实学习材料生成已由记录详情经 `LearningMaterialGenerationActions` 接入；后续请求预览、请求日志、Prompt Preset 自定义执行和 Anthropic / Gemini 学习材料请求体仍需另开任务；Anthropic / Gemini 文本测试和图片 probe 第一阶段返回暂不支持测试，不误报为网络或 API Key 错误；真网人工复核需要可控测试 Provider / API Key；三端语言支持和 TTS 入口需持续保持共享 action seam 和同一隐私边界 |
| 同步设置页 | 预览 iCloud 推荐路径、S3 兼容对象存储高级草稿、同步范围和密钥边界 | 设置列表 `同步` row | Local Mock | `SyncSettingsView.swift`、`SyncSettingsModels.swift`、`SettingsCapabilityDetailView.swift` | 只做真实级 mock UI；iCloud 为推荐路径，S3 兼容对象存储为高级二级表单；照片和音频附件使用本地草稿 switch 开关且默认关闭；生活记录、学习材料、Prompt Preset 和 AI 生成内容作为固定纳入范围展示；向量索引本地重建；AI Key、对象存储密钥和恢复密钥不进入同步；当前不写 Keychain、不写数据库、不发 CloudKit/S3/R2/WebDAV 请求、不上传内容 | 后续真实 Sync Engine / CloudKit Adapter / S3 Adapter 接入前，必须先定义数据模型、manifest、冲突、tombstone、密钥恢复和文档审查 |
| 设置详情 | 查看单项能力的当前状态、下一步和副作用边界 | 设置列表 row | Implemented / Unavailable | `SettingsCapabilityDetailView.swift` | AI Provider 已接入本地配置保存和 Keychain；同步仍是真实级 mock 配置入口；界面语言和外观可切换设备级 preference；不保存真实导入导出配置；设置列表 row 隐藏通用状态 badge，详情页和能力反馈组件仍可使用状态 badge 表达具体状态 | 后续真实 Provider 请求、同步、导入导出接入后逐项复查；设置列表当前值已按 `docs/architecture/notes/2026-05-24-settings-status-projection-notes.md` 在 E12 落地（行尾低干扰真实值经 `SettingsStatusProjection` 投影，渲染零 Keychain / 零网络；开发完成度不再泄漏给用户） |
| 学习画像总览页（LM02-S1） | 查看 App 对你的了解：温和水平、知识覆盖、复习统计、Memory 生活事实/目标（显式记住 + 删除）、系统级重置、盲点（听写错误模式，S3） | 设置列表顶部 `学习画像` 独立导航行（`PhoneRoute.learnerProfile`） | Implemented | `LearnerProfileView.swift`、`LearnerProfilePresentation.swift`、`LearnerProfileStore.swift`、`LearnerProfileActions.swift`、`LearnerProfileSettingsRow`（`PhoneMainSections.swift`） | 独立导航项，**非 SettingsCapability**（画像不是带状态配置能力）；纯本地零外发；温和总体水平**不展示降级**（ADR-006 §10），结构上无 downgrade 表达；Memory 事实显式记住 + 单条软删可撤销 + 系统级「重置 App 对我的了解」物理删（不触 `memory_items`/学习记录，有确认流）；盲点分区为占位（产出信号未就绪）；空态「继续记录以解锁画像」 | 不得展示水平降级；重置文案须明确只清 Memory 事实；盲点未就绪不得伪造条目；后续切片填盲点/band/Style 接入同页 |
| 语伴 Language Companion（LM03-S1 / S2a） | 扎根记录的目标语文本对话练习：单线程多轮、始终目标语、人设枚举、冷启动本地问候（零外发）、失败保输入、删该条及后续/清空；**+ 工具栏显式「提取词汇 / 表达」→ 聊天反哺候选（LM03-S2a）** | 设置开关行（默认 **OFF**）+ 练习 Tab 二级 `CompanionEntryCard` + 记录详情「围绕这条记录对话」（`PhoneRoute.companionChat`，方案 A 种子）；聊天页工具栏 secondary action「提取词汇 / 表达」 | Implemented | `CompanionChatView.swift`、`CompanionChatStore.swift`、`CompanionChatPresentation.swift`、`CompanionChatActions.swift`、`CompanionEntryViews.swift`、`CompanionSettingsToggleRow.swift`、`CompanionConversationEngine` / `CompanionExtractionEngine`（AI）、`GRDBCompanionRepository`（Data） | 默认关闭、关闭即门控隐藏不装配（ADR-008 §2.6）；S1 **零系统自动注入**（不读 Memory/不 FTS）；始终目标语（typed directive 契约）；人设纯枚举防注入；**S2a 提取 = 显式触发重发已存对话（非系统自动注入），仅含 `companionConversation` 类目无新外发，预览显式披露，失败保对话**；逐句 TTS 朗读**暂缓**（加性后续，§18 偏差） | 手工验证关注点：始终目标语回复、模糊输入拟真确认、失败保输入不伪装；提取计数 / 空 / 失败三态 + 来源消息已删除态；不得伪称朗读已实现；Memory 注入（系统自动注入）+ PII scrubbing + 两层控制 = S2b（门控未开） |
| 语言空间快速切换 sheet | 查看当前空间、切换 active 空间、添加学习语言，并进入完整管理页 | 顶部语言空间胶囊 | Implemented | `LanguageSpaceSwitcherSheet.swift`、`PhoneMainView.swift` | 复用 App 层语言空间 lifecycle actions；不直接持有 repository、GRDB queue 或 SQL；新增后沿用 AppSessionState 设为当前空间的语义 | 不退回只读 summary；新增流程避免未经验证的双层 modal；失败反馈受当前 action 返回值边界约束 |
| iPhone 页面 chrome | 为记录、阅读、练习、记忆四个主 Tab 提供统一顶部语言空间入口 + 设置齿轮 | 四个主 Tab 导航栏 | Implemented | `PhonePage`、`PhoneRootContextToolbar.swift` | 四 Tab（含阅读）共用 `phoneRootContextToolbar`：inline 居中标题 + leading 语言 chip（打开快速切换 sheet）+ 可选 trailing 设置齿轮（进入设置列表）；**方向 A 去边框轻量控件**——语言为紧凑文字 `英语`（仅目标语言，spec/002 §54 / §157）+ `chevron.down` 指示，齿轮为裸 `gearshape` 图标，二者均无描边 / 填充、`textSecondary` 低对比、触控热区 ≥44pt；完整 `中文 -> 英语 · B1` 仅作 VoiceOver value（等级不进顶部控件——一门空间一门语言，目标语言已唯一标识当前空间）；导航栏到首个内容保留 20pt 呼吸，回收大标题带；设置详情页经 `showsContextHeader: false` 抑制 | 所有主 Tab 都应保留当前语言空间可见性，gear 不应变成底部 Tab；不恢复正文内 `PhoneContextHeader`；不恢复导航栏描边容器（克制：描边语汇留给内容卡） |
| Unavailable sheet | 搜索等未接入能力的临时承载 | `PhoneSheet.unavailable` 或共享组件调用 | Unavailable | `UnavailableCapabilityView.swift`、`PhoneMainView.swift` | 只解释边界，不执行真实副作用 | 不应用于主学习高频体验；能 mock 的能力优先做真实级 local mock |

## 4. iPad 页面清单

iPad 的顶层结构是工作台：顶部工具条、左侧时间线 / 筛选、中央写作与详情主区、右侧学习面板。当前代码事实源是 `PadMainView.swift`、`PadMainSections.swift`、`PadMainModels.swift`、`PadWorkspaceBar.swift`、`PadSidebarControls.swift` 和共享详情 / 练习组件。

| 页面 | 用户目的 | 入口 | 当前状态 | 主要代码路径 | 能力边界 | 审查关注点 |
| --- | --- | --- | --- | --- | --- | --- |
| iPad Main Workspace | 承载三栏工作台和可收起面板 | Root 进入 iPad main | Shell | `PadMainView.swift` | 使用本地 seed；面板展开状态是 transient UI state | Split View、Stage Manager 和窄宽度下主内容优先 |
| 顶部工作台工具条 | 切换左右面板、搜索、新建记录 | iPad main 顶部 | Implemented / Unavailable | `PadWorkspaceBar.swift` | 搜索打开 unavailable；新建打开编辑 sheet；设置入口不在顶部重复出现 | 图标按钮需保持 44pt 触控和动态辅助标签，右侧面板切换位于顶部最右侧 |
| 左侧时间线 | 浏览记录并选择当前 entry | 左侧面板 | Implemented | `PadSidebarView`、`EntryTimelineRow` | 使用共享 learning content store；选择 entry 后主区通过共享 `EntryDetailView` 展示真实学习材料生成入口和状态 | 收起后不留空白；后续复查 iPad async 保存和平台人工验收 |
| 左侧筛选 | 按全部、照片写作、待练习筛选 | 左侧面板 | Implemented | `EntryTimelineFilter` in `EntryTimeline.swift`、`PadSidebarView` | 使用真实 `practiceReadiness` 来自 GRDB；全部 / 照片写作 / 待练习三枚 chip，已记忆 chip 待 E7 显示；日期分组与 iPhone 共享 `groupEntriesByDay` | 已移除旧 `PadFilter` enum；筛选计数每次从 `practiceReadiness` 推导 |
| 左侧页面入口 | 进入阅读、记忆、导入导出、设置 | 左侧面板 | Implemented / Unavailable | `PadSidebarView`、`PadWorkspaceRoute` | 阅读为真实资料库 route；导入导出为 unavailable；设置为只读能力页 | 不把低频配置放到顶部挤压工作台 |
| iPad 语言空间底部区 | 显示当前空间、AI、同步、设置入口 | 左侧面板底部 | Implemented | `LanguageSpaceFooter.swift` | AI / Sync 为状态说明，不自动发送或同步；**E12 起 footer 的 AI / 同步状态由 `SettingsStatusProjection` 真实投影驱动**（不再硬编码 `.notConfigured` / `.off`，修复 UIV-08），AI 反映已配置 / 缺密钥 / 未配置，同步反映未启用 | 图标不能只靠颜色表达状态 |
| 工作台概览 / 写作主区 | 展示当前记录详情、学习材料生成入口、双文本卡片和句子列表 | 默认 route `.workspace` | Implemented | `PadWorkspaceContentView.workspaceOverview`、`EntryDetailView` | 默认选中记录和 route detail 共用同一详情组装，真实生成、取消、learning text 编辑和重新分析 action 均来自 `LearningContentStore` / `LearningMaterialGenerationActions`；逐句 `听` 通过 `SentenceAudioPlaybackActions` 接入真实 TTS 生成 / 缓存 / 播放；逐句 `练` 通过共享 practice route seed 进入单句跟读录音 | 中央主区是视觉焦点；避免卡片套卡片；后续人工验收 iPad 常规宽度和窄宽度阅读节奏 |
| iPad 空工作台状态 | 没有选中记录时给出安静的主区占位 | 默认 route 且无 selected entry，或目标 entry 缺失 | Implemented | `EmptyWorkspacePanel` in `PadSidebarControls.swift` | 不创建记录、不生成内容 | 不能显示裸文本或错误式空状态；后续真实空数据需保留创建入口 |
| 记录详情 | 在主区完整查看单条记录 | 时间线选择或 route `.entryDetail` | Implemented | `EntryDetailView` reused in `PadWorkspaceContentView` | 与 iPhone 共享详情组件，支持动态文本卡片、原文编辑、旧记录状态展示、真实生成、取消、learning text 编辑和重新分析 | 共享组件改动需同时复查 iPhone 和 iPad；仍需平台人工验收大屏状态展示 |
| 阅读资料库 route | 管理当前语言空间阅读资料并阅读 | 左侧 `阅读` | Implemented / Reading CRUD / AI / TTS / Inspector Fold | `PadWorkspaceContentView`、`ReadingLibraryView`、`ReadingLibraryStore`、`ReadingDocumentStore` | iPad 阅读工作台采用”资料库侧栏 + 中央阅读画布 + 次级学习 inspector”三栏结构；支持资料库列表、搜索、`全部 / 收藏` chips 筛选、文档行 `字数·导入日期·进度` 元信息与收藏星标、粘贴导入、`.txt` / `.md` 文件导入、collection / tag 赋值和筛选、打开、标题 / 正文编辑、删除 / 恢复、Markdown / 纯文本阅读、句子级显式选择解释、reading sentence TTS；right inspector 可通过顶部按钮折叠，折叠后降级为画布底部 compact 面板（`选中 → 解释` 路径保持可用），折叠 / 展开不触发 AI 或 TTS；inspector 与 compact 面板均含三档解释语言模式切换；不自动发送全文或自动播放 | 后续仍需强化真实选择引擎、阅读进度滚动采集与恢复、Split View / Stage Manager 人工验收和 Dynamic Type / VoiceOver 验收；不回退到三列同权挤压主阅读区的验证态布局 |
| 听一句原位播放 | 从句子列表触发单句 TTS 播放或生成后播放 | 句子 `听` | Implemented / TTS Playback | `SentencePairView`、`SentencePairActionControls.swift`、`SentenceAudioPlaybackActions`、`LearningContentStore` | 不打开解释型 sheet；通过共享 action contract 接入同一 coordinator，保持 cache hit 优先、单句显式点击外发和跨句互斥播放边界 | 后续真实录音、听写、跟读评分、后台播放和全文朗读不得复用该按钮职责 |
| 练习详情 | 在主区进行单句跟读录音 | 句子 `练` 或学习面板练习入口 | Implemented / Practice Recording | `PracticeSentenceListView`、`PracticeSessionView`、`PracticePromptCard.swift`、`PracticeControlBar.swift`、`PracticeActions.swift` | iPad 共享同一 `PracticeSessionRouteSeed`、practice repository、recording service、示范 TTS playback coordinator、prompt card disclosure UI state 和媒体资产写入 / 回放路径；学习面板有可练习句子时入口显示 ready，不再标为 mock；当前主流程为记录 -> 句子列表 -> 单句跟读 / 听示范 / 录音 / 回放 / 再录一次 | 大屏布局和 Stage Manager resize 需要人工验收；后续键盘快捷键和评分不得分叉业务状态 |
| 设置列表 | 在主区查看能力列表 | 左侧设置、底部设置 | Implemented | `PadWorkspaceContentView.settingsList` | 多数设置为只读说明，界面语言可切换；设置列表隐藏通用状态 badge，不展示开发阶段 completion marker | iPad 设置应服务当前空间，不变成后台管理；顶部工具条不重复放置设置齿轮 |
| 设置详情 | 查看单项能力边界 | 设置列表 row、底部 AI / Sync | Implemented / Local Mock / Unavailable | `SettingsCapabilityDetailView.swift`、`SyncSettingsView.swift` | AI Provider 详情共享同一表单并可保存本地 Keychain 配置，作为系统级配置不显示当前语言空间归属 header；配置测试会使用当前语言空间 target language code；外观和界面语言为全局设备级偏好，无语言空间时仍可展示；无真实 sync、export 写入；同步详情使用共享 Local Mock UI，iPad 常规宽度默认进入设置焦点并收起右侧学习面板，窄宽度回退单列；设置列表 row 隐藏通用状态 badge，详情页和能力反馈组件仍可使用状态 badge 表达具体状态 | 与 iPhone 设置详情共享，改动需三端复查；AI Provider 的当前语言空间只作为 language support / voice profile 上下文，不进入 profile 静态事实；设置焦点是 transient UI state，不进入语言空间、数据库或同步 manifest；设置列表当前值已按 `docs/architecture/notes/2026-05-24-settings-status-projection-notes.md` 在 E12 落地（行尾低干扰真实值经 `SettingsStatusProjection` 投影，渲染零 Keychain / 零网络；开发完成度不再泄漏给用户） |
| 记忆页 | 查看记忆摘要、记忆项和向量索引边界 | 左侧 `记忆` | Local Mock / Unavailable | `PadWorkspaceContentView.memoryPage`、`MemoryLayerSummaryView` | 向量索引为 unavailable；memory items 来自本地 mock | iPad 可展示较多结构，但不能压过学习主线 |
| 学习画像总览页（LM02-S1） | 查看 App 对你的了解：水平、覆盖、复习、Memory 事实增删、系统级重置、盲点（听写错误模式，S3） | 设置列表顶部 `学习画像` 行（`PadWorkspaceRoute.learnerProfile`）；侧栏设置区 peer | Implemented | `LearnerProfileView.swift`（三端共享）、`PadWorkspaceContentView`（settingsList 行 + route 渲染）、`PadMainModels.swift` | 与 iPhone 共享同一 `LearnerProfileView`；侧栏 peer 项，非 SettingsCapability；纯本地零外发，不展示降级；右侧学习面板对 `.learnerProfile` 复用设置上下文 | 与 iPhone / macOS 共享视图，改动三端复查；不得展示降级；不在主区外另起重复入口 |
| 语伴 Language Companion（LM03-S1） | 同 iPhone（三端共享 `CompanionChatView`/引擎/Store） | 设置开关行（默认 **OFF**）+ 记录详情「围绕这条记录对话」（`PadWorkspaceRoute.companionChat`，方案 A）；Pad 工作区 entry-driven 无独立练习 landing，通用入口经记录详情 | Implemented | `CompanionChatView.swift`（三端共享）、`PadMainSections`（settingsList 开关行 + entryDetail 按钮 + route 渲染）、`PadMainModels.swift`、`PadLearningPanelView`（companion 上下文面板） | 与 iPhone/macOS 共享引擎；默认关闭门控；S1 零系统自动注入；不伪称朗读；**S2a 提取动作三端共享 `CompanionChatView` 工具栏（显式触发非自动注入）** | 三端共享改动复查；Pad 通用入口经记录详情（决策 #2 按设备设计），非功能缺失；Memory 注入 + PII scrubbing = S2b（门控未开） |
| 导入导出页 | 说明导入导出尚未接入 | 左侧 `导入导出` | Unavailable | `PadWorkspaceContentView.importExportPage`、`UnavailableCapabilityView` | 不打开文件、不读写导出包 | 后续真实文件访问需权限和存储方案 |
| 语言空间管理页 | 查看所有语言空间，新增、切换、编辑和删除空间 | 底部语言空间入口、设置列表语言空间 row | Implemented | `PadWorkspaceContentView.languageSpaceManagementPage`、`LanguageSpaceManagementView.swift`、`LanguageSpaceEditorView.swift` | 复用 App 层语言空间 lifecycle actions；不改变 Data 层同目标语言多空间策略；删除沿用 repository fallback 语义 | 管理页应保持 iPad 主区阅读宽度和触控友好操作，不应退化为 iPhone sheet 的放大版 |
| 右侧学习面板 | 随当前 route 展示上下文；记录 / 练习 route 展示当前句子、记忆提取、练习入口、空间设置、请求预览 | 右侧面板 | Implemented / Real Projection (E6) | `PadMainView.learningPanel`、`PadLearningPanelView` | `RequestPreviewCard` 只出现在 entry 学习上下文；设置、记忆、导入导出和语言空间 route 显示对应低干扰上下文，不发送数据；练习 route 可展示当前单句上下文但不直接持有 recorder 或文件路径 | 右侧内容必须匹配主区 route，不能在设置页继续展示当前记录学习内容；右侧面板容器需保留外侧 gutter，内容块需保留额外 trailing inset，避免贴近屏幕边缘 |
| 右侧学习面板空状态 | 未选中记录时说明学习面板等待内容 | 右侧学习面板无 selected entry | Implemented | `PadLearningPanelView`、`LocalizedTextPanel` | 只展示说明，不触发生成或练习 | 空状态应低干扰，不能要求用户先配置工程能力 |
| 搜索 unavailable sheet | 表达搜索尚未接入 | 顶部搜索 | Unavailable | `PadSheet.unavailableSearch`、`UnavailableCapabilityView(.search)` | 不执行真实搜索或索引查询 | 后续搜索接入时需定义跨空间范围 |
| 新建记录 sheet | 创建文本记录 | 顶部新建 | Implemented / Platform Follow-up | `EntryEditorView` reused by `PadMainView` | 复用共享 store；保存后进入共享详情能力，生成学习材料 action 已接入 | 后续 iPad 可考虑平台专属编辑体验；复查创建保存状态和失败恢复 |

## 5. macOS 页面清单

macOS 的顶层结构是桌面工作台：左侧 Sidebar、中央主区、右侧 Inspector、Toolbar、菜单命令和原生 Settings scene。当前代码事实源是 `MacMainView.swift`、`MacMainModels.swift`、`MacWorkspaceContentView.swift`、`MacInspectorContent.swift`、`MacEntryEditorSheet.swift`、`LangoTraceSettingsSceneView.swift` 和 `LangoTraceApp.swift`。

| 页面 | 用户目的 | 入口 | 当前状态 | 主要代码路径 | 能力边界 | 审查关注点 |
| --- | --- | --- | --- | --- | --- | --- |
| Mac Main Workspace | 承载 Sidebar、主区、Inspector 和 toolbar | Root 进入 macOS main | Shell | `MacMainView.swift` | 使用本地 seed；Sidebar / Inspector 展开状态为 transient UI state | 最小窗口宽度、菜单命令和键盘路径需持续复查 |
| Mac Sidebar | 在 Today、Entries、Reading、Practice、Memory、Import / Export、Settings 间切换 | 左侧 Sidebar | Implemented | `MacWorkspaceSection`、`MacSidebarItem` | Reading section 进入真实资料库 route；Today / Entries subtitle 通过 `timelineCounts(entries:practiceReadiness:today:)` 计算真实今日条数和总条数；其他 section selection 不直接创建业务数据 | 不使用移动端 Tab；hover、focus、context menu 需保留 |
| Mac Toolbar | 切换 Sidebar / Inspector、搜索、新建记录 | 窗口 toolbar | Implemented / Unavailable | `MacMainView.toolbar` | 搜索 route 为 unavailable；新建打开 overlay | 桌面命令需与菜单栏保持一致 |
| Today 主区 | 展示当前 entry 详情和搜索 / 批量导入入口 | Sidebar `Today` | Implemented / Unavailable mix | `MacWorkspaceContentView.todayContent`、`EntryDetailView` | 当前 entry 通过共享详情接入真实生成、取消、learning text 编辑和重新分析；搜索、批量导入为 unavailable | 不能像后台 dashboard，主内容应围绕当前记录；后续人工验收桌面阅读密度 |
| Entries 资料库 | 浏览所有记录并选择详情 | Sidebar `Entries` | Implemented | `MacWorkspaceContentView.entriesContent` | 使用本地 entries | 后续批量管理、排序、搜索接入后更新 |
| Entry Detail | 查看选中记录详情 | Entries row、Today 选中、保存后 | Implemented | `EntryDetailView` reused in `MacWorkspaceContentView` | 共享详情组件，支持动态文本卡片、原文编辑、旧记录状态展示、真实生成、取消、learning text 编辑和重新分析 | macOS 可能需要更桌面化详情布局，避免移动布局直接放大；后续人工验收 AI Provider 披露和右侧 Inspector 元数据 |
| Reading section | 管理阅读资料库并阅读文档 | Sidebar `Reading` 或 route `.reading` | Implemented / Reading CRUD / AI / TTS | `MacWorkspaceContentView`、`ReadingLibraryView`、`ReadingLibraryStore`、`ReadingDocumentStore`、`ReadingSelectionExplanationService`、`SentenceAudioPlaybackActions` | macOS 阅读页采用三栏工作台：左栏是资料库与导入管理（含 `全部 / 收藏` chips 筛选、文档行 `字数·导入日期·进度` 元信息与收藏星标），中栏是独立阅读画布，右栏是常驻学习 inspector（含三档解释语言模式切换，macOS inspector 不支持折叠）；支持列表、搜索、粘贴导入、`.txt` / `.md` open panel 导入、collection / tag 赋值和筛选、打开、标题 / 正文编辑、软删除 / 恢复、Markdown / 纯文本阅读、句子级显式 selection explanation 和 `readingDocumentSentence` TTS；解释请求与 iPhone / iPad 对齐同一 sentence-first / dynamic-context seam；不做 EPUB/PDF/HTML、全文总结、全文朗读或后台播放 | 后续 Mac 仍应补 toolbar / command、键盘 fragment selection 和长文性能人工验收；不回退到视觉统一但职责不清的旧三栏结构 |
| New Entry Overlay | 桌面化创建记录 | Toolbar plus、菜单 New Entry | Implemented / Platform Follow-up | `MacEntryEditorOverlay`、`MacEntryEditorSheet.swift` | 复用共享 store；保存后进入共享详情能力，生成学习材料 action 已接入 | overlay 点击背景取消、键盘取消和输入体验需保持；后续复查 async 保存和失败恢复 |
| Practice section | 查看所有 entry 的练习入口 | Sidebar `Practice` | Implemented / Practice Recording | `MacWorkspaceContentView.practiceContent`、`PhonePracticeRows.swift` | 有 rendering 的记录显示 ready 练习卡片，点击后进入句子列表；不混排未实现的听写 / 回译任务 | 桌面密度和键盘操作仍需后续人工验收；不能把真实同步 / 导出录音写成已完成 |
| Practice route | 执行单句跟读录音会话 | Practice item、句子列表或 entry detail | Implemented / Practice Recording | `PracticeSentenceListView`、`PracticeSessionView`、`PracticePromptCard.swift` reused in `MacWorkspaceContentView.practice` | macOS 复用同一 practice action seam、GRDB practice repository、示范 TTS playback coordinator、prompt card disclosure UI state、App 管理媒体资产、录音回放 resolver 和麦克风权限配置；练习列表和单句页使用 dedicated main scrolling，避免被工作台外层 `ScrollView` 再包裹；录音默认本地，不自动上传、不默认导出 | 后续需要桌面 toolbar / command / shortcut 设计和真实设备麦克风验收；当前不做后台录音、ASR 或评分 |
| Memory section | 查看记忆摘要、记忆项和向量索引边界 | Sidebar `Memory` | Local Mock / Unavailable | `MacWorkspaceContentView.memoryContent`、`MemoryLayerSummaryView` | 向量索引为 unavailable | macOS 可展示更多资料库信息，但不能泄露工程术语给普通用户 |
| 学习画像总览页（LM02-S1） | 查看 App 对你的了解：水平、覆盖、复习、Memory 事实增删、系统级重置、盲点（听写错误模式，S3） | Settings section 顶部 `学习画像` 行（`MacWorkspaceRoute.learnerProfile`） | Implemented | `LearnerProfileView.swift`（三端共享）、`MacWorkspaceContentView`（settingsContent 行 + route 渲染）、`MacMainModels.swift`、`MacInspectorContent`（learnerProfile inspector） | 与 iPhone / iPad 共享同一 `LearnerProfileView`；Settings 区 peer 行，非 SettingsCapability；纯本地零外发，不展示降级；inspector 对 `.learnerProfile` 显示画像说明 | 与 iPhone / iPad 共享视图，改动三端复查；Settings section 与原生 Settings scene 职责不冲突；不得展示降级 |
| 语伴 Language Companion（LM03-S1） | 同 iPhone（三端共享 `CompanionChatView`/引擎/Store） | 设置开关行（默认 **OFF**）+ 练习 section 顶部 `CompanionEntryCard` + 记录详情按钮（`MacWorkspaceRoute.companionChat`，方案 A） | Implemented | `CompanionChatView.swift`（三端共享）、`MacWorkspaceContentView`（practice 区入口 + settingsContent 开关行 + entryDetail 按钮 + route）、`MacMainModels.swift`、`MacInspectorContent`（companion inspector） | 与 iPhone/iPad 共享引擎；默认关闭门控；S1 零系统自动注入；macOS Settings scene 亦注入开关；不伪称朗读；**S2a 提取动作三端共享 `CompanionChatView` 工具栏（显式触发非自动注入）** | 三端共享改动复查；不得堆第四 Tab/顶层导航（ADR-008 §3）；Memory 注入 + PII scrubbing = S2b（门控未开） |
| Import / Export section | 说明导入导出尚未接入 | Sidebar `Import / Export` 或 bulk import action | Unavailable | `MacUnavailableContent("import-export")`、`UnavailableCapabilityView` | 不打开文件、不导入、不导出 | 后续真实能力需文件访问、附件、导出格式方案 |
| Settings section | 查看能力设置列表 | Sidebar `Settings` 或 `Cmd+,` 通知后选中 settings | Implemented | `MacWorkspaceContentView.settingsContent` | 多数能力只读；界面语言和外观可切换设备级 preference；设置列表隐藏通用状态 badge，不展示开发阶段 completion marker | Settings section 和原生 Settings scene 的职责不能冲突 |
| Settings detail route | 查看单项能力详情 | Settings row、Sidebar footer AI / Sync | Implemented / Local Mock / Unavailable | `SettingsCapabilityDetailView.swift`、`SyncSettingsView.swift`、`MacWorkspaceContentView.swift` | AI Provider 详情共享同一表单并可保存本地 Keychain 配置，作为系统级配置不显示当前语言空间归属 header；配置测试会使用当前语言空间 target language code；外观和界面语言为设备级全局偏好；无真实同步、导出配置写入；同步详情共享 Local Mock UI，并在 macOS 工作台使用 embedded presentation 避免与外层 `ScrollView` 嵌套；设置列表 row 隐藏通用状态 badge，详情页和能力反馈组件仍可使用状态 badge 表达具体状态 | 真实 Provider 请求 / Sync / StoreKit 接入时逐项复查；AI Provider 的当前语言空间只作为 language support / voice profile 上下文，不进入 profile 静态事实；工作台 Settings 和原生 Settings scene 都只是设置入口，不承载学习主流程；设置列表当前值已按 `docs/architecture/notes/2026-05-24-settings-status-projection-notes.md` 在 E12 落地（行尾低干扰真实值经 `SettingsStatusProjection` 投影，渲染零 Keychain / 零网络；开发完成度不再泄漏给用户） |
| Language Space management route | 查看所有语言空间，新增、切换、编辑和删除空间 | Sidebar footer 语言空间、Settings section 语言空间 row | Implemented | `MacWorkspaceContentView.swift`、`LanguageSpaceManagementView.swift`、`LanguageSpaceEditorView.swift` | 复用 App 层语言空间 lifecycle actions；右键菜单提供编辑和删除；删除沿用 repository fallback 语义 | 多窗口 / 多语言空间设计前必须复查当前空间切换对窗口上下文的影响 |
| Search unavailable route | 表达搜索尚未接入 | Toolbar search、菜单 search | Unavailable | `MacUnavailableContent("search")`、`UnavailableCapabilityView` | 不查询 FTS、向量索引或外部服务 | 后续搜索需要 Command Palette / 全局搜索边界 |
| Missing route fallback | 当前 route 指向缺失 entry、practice 或 setting 时给出可读反馈 | 深层 route 找不到对象 | Implemented | `MacWorkspaceContentView.entryDetail`、`practice`、`settingDetail`、`LocalizedCompactPanel` | 不自动恢复、创建或删除数据 | 后续真实持久化后需要明确 missing object 恢复策略 |
| Inspector Overview | 根据当前 section 展示上下文说明 | 右侧 Inspector，route `.overview` | Implemented | `MacInspectorContent.overviewInspector` | 说明性上下文，不写入数据 | Inspector 文字不能替代主区可用性 |
| Inspector Entry | 展示 entry metadata、请求预览、记忆候选、隐私边界 | route `.entryDetail` | Implemented / Real Projection (E6) | `MacInspectorContent`、`RequestPreviewCard`、`AIRequestLogListView` | Request preview 仍是本地 / 显式请求边界展示 | macOS 可承载请求预览，但必须避免暗示自动发送 |
| Inspector Practice | 展示练习状态和练习上下文 | route `.practiceSentenceList` / `.practiceSentence` | Implemented / Local Projection | `MacInspectorContent` | 只展示 route 级练习上下文和状态投影，不持有 recorder、文件 URL 或 GRDB repository | 录音、完成和失败状态仍以主区 `PracticeSessionView` / action seam 为准；后续评分和快捷键另开方案 |
| Inspector Settings | 展示设置说明和下一步 | route `.settings` | Implemented / Unavailable | `MacInspectorContent` | 读取 setting capability 文案 | 避免三块式工程说明过重 |
| macOS Settings scene | 系统级设置窗口，展示能力状态列表、语言空间管理和能力详情 | App Settings / `Cmd+,` | Implemented / Local Mock | `LangoTraceApp.swift` Settings scene、`LangoTraceSettingsSceneView.swift` | 读取当前环境的 settings capabilities；能力列表隐藏通用状态 badge；语言空间 row 进入完整管理页并复用 App 层 lifecycle actions；外观和界面语言无当前语言空间时仍可进入；有当前语言空间时可选择同步等能力并展示共享 `SettingsCapabilityDetailView`；无语言空间时仍可从语言空间管理页新增空间，不创建默认空间、不保存真实空间配置 | 原生 Settings 和工作台 Settings section 需要保持语义一致；无语言空间状态不能使用 `bootstrap` 能力冒充当前空间配置；外观偏好只写设备级 `UserDefaults`，后续真实同步或多主题写入要单独设计 |
| macOS Commands | 菜单触发 New Entry、Search、Toggle Sidebar、Toggle Inspector、Settings | 菜单栏 / 快捷键 | Implemented / Unavailable | `LangoTraceApp.swift`、`LangoTraceAppCommand.swift` | Search 打开 unavailable，不执行真实搜索 | 菜单项必须有可见结果，不能空 action |

## 6. 共享页面与组件清单

这些组件不是独立平台入口，但它们承载多个页面的核心体验。修改时必须按影响平台复查。

| 组件 / 页面 | 使用平台 | 当前状态 | 主要代码路径 | 影响范围 |
| --- | --- | --- | --- | --- |
| `EntryDetailView` | iPhone / iPad / macOS | Implemented | `PhoneMainSupportingViews.swift` | 三端记录详情、真实学习材料生成入口、生成中取消、learning text 编辑、重新分析、逐句练习、未 rendering 状态 |
| `SentencePairView` | iPhone / iPad / macOS | Implemented / TTS Playback | `LearningContentComponents.swift`、`SentencePairActionControls.swift`、`SentenceAudioPlaybackActions.swift` | 单句听读原位播放和练习入口；当前通过注入 action contract 接入真实 TTS generation / local artifact cache / playback coordinator，不打开听力解释 sheet |
| `PracticeSentenceListView` / `PracticeSessionView` | iPhone / iPad / macOS | Implemented / Practice Recording / Dictation / Backtranslation | `PhoneMainSupportingViews.swift`、`PracticeRouting.swift`、`PracticeSentenceListPresentation.swift`、`PracticePromptCard.swift`、`PracticeShadowingLayout.swift`、`PracticeSessionViewModel.swift`、`PracticeDictationSessionViewModel.swift`、`PracticeDictationSessionView.swift`、`PracticeBacktranslationSessionViewModel.swift`、`PracticeBacktranslationSessionView.swift`、`PracticeControlBar.swift`、`PracticeActions.swift` | 记录练习句子列表、跟读 / 听写 / 回译 mode availability、按 exercise type 计算的已练状态（跟读=ready recording、听写 / 回译=文本 attempt）、续练 CTA、单句 route seed、句子快照、目标句优先的 prompt card、同行 disclosure controls、`查看释义` 按需显示完整中文释义、讲解按段落显示、听示范、跟读录音 / ready recording 回放 / 可重复重录、听写本机对照（`PracticeDictationDiff`）、回译本地参考对照（`sentenceAnalysis` seam 懒加载 + snapshot 兜底）和三端共享 action seam；`PracticeSessionView` 按 exercise type 分发到 `PracticeShadowingSessionView` / `PracticeDictationSessionView` / `PracticeBacktranslationSessionView` |
| `SettingsCapabilityDetailView` | iPhone / iPad / macOS | Implemented / Local Mock / Unavailable | `SettingsCapabilityDetailView.swift` | AI、同步、隐私、导入导出、本地数据、语言空间、界面语言、外观；AI Provider 隐藏通用内容 header，外观和界面语言支持无语言空间的 global detail，其他空间相关能力保持 no-space boundary；支持 standalone scroll 和 embedded presentation，供 macOS 工作台避免嵌套滚动 |
| `AIProviderSettingsView` | iPhone / iPad / macOS | Implemented / Configuration Synthetic Probe / TTS / Embedding Probe | `AIProviderSettingsView.swift`、`AIProviderDraftConfiguration.swift`、`AIProviderSettingsActions.swift`、`AIProviderSettingsModels.swift`、`AIProviderSettingsComponents.swift` | 文本模型、语音生成模型、向量模型 endpoint；表单本身不渲染内容区标题或当前语言空间副标题，由外层导航标题承担页面归属；语音和向量可共享文本模型 API Key 或使用独立 API Key；语音生成模型通过“语音细节”子区显示 voice、format、speed、speaking style，并保存当前语言空间 voice profile；保存配置写 SQLite / GRDB metadata 与 Keychain secret；加载已保存配置时不回填 Keychain secret，只显示本机已保存 placeholder，用户点击小眼睛后才解析并回填到同一可编辑 UI draft；页面消失、进入后台、切换 Provider / credential reference 或保存成功会清空短生命周期明文；同时按当前 language code 读取 TTS voice profile 回填语音字段；测试按钮走共享 action seam，打开共享分能力结果内容；文本测试 readiness 与保存 readiness 分离；未保存 draft 测当前屏幕配置，已保存且无修改时由服务层重新解析 Keychain；有当前语言空间时三端都会传入 target language code 并请求 `语言支持`，启用且配置完整时也会请求固定低敏 TTS 测试；成功 TTS 结果可经 Speech preview playback seam 试听短生命周期样例音频；iPhone compact 使用 bottom sheet detents，iPad 常规宽度和 macOS 不强制套用移动端 detents；结果面板内容在大屏使用固定最大宽度；iPad / macOS 由 `SettingsCapabilityDetailView` 负责表单最大内容宽度，不分叉表单组件 |
| `SyncSettingsView` | iPhone / iPad / macOS | Local Mock | `SyncSettingsView.swift`、`SyncSettingsModels.swift`、`SyncS3DraftView.swift` | iCloud 推荐、S3-compatible 高级草稿、同步范围和密钥边界；照片和音频附件是唯一可切换草稿项并显式使用 switch；iPad / macOS 常规宽度使用自适应多栏，compact / 窄宽度回退单列；当前不连接 CloudKit、S3、R2、WebDAV、数据库或 Keychain |
| `LanguageSpaceSwitcherSheet` | iPhone | Implemented | `LanguageSpaceSwitcherSheet.swift` | iPhone 顶部语言空间快速切换、添加学习语言和完整管理入口 |
| `LanguageSpaceManagementView` / `LanguageSpaceEditorView` | iPhone / iPad / macOS | Implemented | `LanguageSpaceManagementView.swift`、`LanguageSpaceEditorView.swift` | 语言空间列表、当前态、新增、切换、编辑和删除确认；三端共享管理语义，平台外壳负责入口和承载 |
| `UnavailableCapabilityView` | iPhone / iPad / macOS | Unavailable | `UnavailableCapabilityView.swift` | 搜索、导入导出、向量索引和通用未接入能力 |
| `RequestPreviewCard` | iPad / macOS | Implemented / Real Projection (E6) | `LearningContentComponents.swift`、`AIRequestPreviewPresentation.swift` | iPad 学习面板、macOS Inspector |
| `AIRequestLogListView` | iPad / macOS / 设置 | Implemented / Real Data (E6) | `AIRequestLogListView.swift` | iPad 学习面板、macOS Inspector、设置 AI Provider 详情 |
| `MemoryLayerSummaryView` | iPad / macOS | Local Mock / Unavailable mix | `LearningContentComponents.swift` | 记忆摘要和向量索引边界 |
| `LanguageSpaceFooter` | iPad / macOS | Implemented | `LanguageSpaceFooter.swift` | Sidebar 底部空间、AI、同步、设置入口 |
| `CapabilityStatusRow` | iPhone / iPad / macOS | Implemented | `LearningContentComponents.swift` | 能力状态、设置列表、unavailable / local mock 入口 |
| `PhonePage` / `phoneRootContextToolbar` | iPhone | Implemented | `PhoneMainSections.swift`、`PhoneRootContextToolbar.swift` | iPhone 主 Tab 的页面外壳与四 Tab 共享的导航栏上下文条（语言胶囊 + 设置齿轮，方向 B 统一描边控件） |
| `EntryTimelineRow` | iPad / macOS | Implemented | `LearningContentComponents.swift` | iPad 时间线、macOS entries library 的记录选择行 |
| `AudioPanel` | iPad | Local Mock | `ContentUtilityComponents.swift` | iPad 工作台概览中的本地音频视觉占位，不播放真实音频 |
| `EmptyWorkspacePanel` / `LocalizedCompactPanel` / `LocalizedTextPanel` | iPad / macOS | Implemented | `PadSidebarControls.swift`、`ContentUtilityComponents.swift`、`LocalizedTextPanel.swift` | 空状态、缺失对象状态和说明型面板 |
| `LangoTraceSettingsSceneView` | macOS | Implemented | `LangoTraceSettingsSceneView.swift` | 原生 Settings scene 的能力列表、语言空间管理和共享能力详情；能力列表隐藏开发阶段通用状态 badge；外观与界面语言无当前语言空间时仍可进入 |

## 7. 页面覆盖自检

截至 2026-05-23，本文档已按以下代码事实逐项覆盖：

- Root phase：`LangoTraceAppPhase.welcome`、`onboarding`、`main`。
- iPhone route：`PhoneRoute.entryDetail`、`practiceSentenceList`、`practiceSentence`、`settings`、`settingsList`。
- iPhone sheet：`PhoneSheet.entryEditor`、`photoWriting`、`unavailable`、`languageSpaceSwitcher`。
- iPad route：`PadWorkspaceRoute.workspace`、`entryDetail`、`practiceSentenceList`、`practiceSentence`、`settingsList`、`settings`、`memory`、`importExport`、`languageSpaceManagement`。
- iPad sheet：`PadSheet.entryEditor`、`unavailableSearch`。
- macOS section：`MacWorkspaceSection.today`、`entries`、`practice`、`memory`、`importExport`、`settings`。
- macOS route：`MacWorkspaceRoute.overview`、`entryDetail`、`practiceSentenceList`、`practiceSentence`、`settings`、`languageSpaceManagement`、`unavailable`。
- macOS command：`newEntry`、`search`、`toggleSidebar`、`toggleInspector`、`showSettings`。
- macOS scene：main `WindowGroup` 和原生 `Settings` scene。
- 外观偏好：`AppearancePreference`、`SettingsCapability.Kind.appearance`、三端共享 `SettingsCapabilityDetailView` 和 App 层 `preferredColorScheme`。

## 8. 当前不应出现的页面表现

以下表现如果重新出现，应视为 UI 质量或文档一致性问题：

- iPhone 顶层恢复五 Tab 或把设置作为底部 Tab。
- iPhone 记录 Hero 恢复 `听` 作为首屏高频入口。
- iPhone 记录详情默认显示持久 `RequestPreviewCard`。
- 逐句 `听` 按钮重新打开解释型 sheet 或 `LocalListeningPreviewView`。
- iPhone 记忆页展示向量索引、embedding 或三层技术基础设施。
- 任何用户主流程显示 `规划中`、`待配置`、`当前页面只展示入口边界`、`不播放真实 TTS` 这类开发排期文案。
- iPhone 练习 Tab 恢复“从生活进入练习”标题，或把听写 / 回译等未实现任务类型混排到首层。
- 记录详情在句子列表后恢复“进入练习”、反向翻译或跟读等 practice candidate 信息卡；真实进入练习应通过逐句 `练` 或练习列表完成。
- 练习 route 重新退回 Entry 级 `.practice(entryID)`，导致单句 session 无法携带稳定 sentence identity。
- 能使用本地模拟做真实级展示的能力，退回到大段 unavailable 说明。
- iPad 或 macOS 主工作区退化成放大的 iPhone Tab。
- macOS 菜单命令或 toolbar 按钮出现空 action。
- 设置页把真实密钥、同步、导出或外部请求能力伪装成已接入。

## 9. 维护规则

新增或修改页面时，开发计划必须回答：

1. 这个页面属于 iPhone、iPad、macOS 还是共享组件？
2. 它是 Root、主流程、设置流程、临时任务、Inspector、Sheet、Unavailable 还是 Local Mock？
3. 它的入口在哪里？是否有返回路径或关闭路径？
4. 它是否创建、修改、导入、导出、发送、录音、读取照片或访问文件？
5. 它的当前状态应该是 `Implemented`、`Local Mock`、`Unavailable`、`Shell` 还是 `Planned`？
6. 对应 SwiftUI 文件和测试文件是什么？
7. 是否需要同步更新 `docs/spec/002-navigation-and-routing.md`、`docs/spec/003-ui-design-system.md`、`docs/spec/010-apple-platform-interaction-and-accessibility.md` 或 `docs/review/INDEX.md`？

完成页面相关任务前，至少检查：

```bash
rg "struct .*View|enum .*Route|enum .*Sheet|NavigationStack|TabView|sheet\\(|navigationDestination|Settings" Packages/LangoTraceUI/Sources/LangoTraceUI LangoTraceApp -n
rg "规划中|待配置|当前页面只展示入口边界|不播放真实 TTS|LocalListeningPreviewView|isListeningPreviewPresented|UnavailableCapabilityView\\(content: \\.listenOne\\)" Packages/LangoTraceUI/Sources Packages/LangoTraceUI/Tests
```

如果页面清单和代码不一致，以代码为当前事实，并在同一任务中修正文档或明确记录偏差。

## 10. 变更记录

- 2026-05-19：创建第一版三端页面清单。依据当前 SwiftUI 代码记录 Root、iPhone、iPad、macOS、共享组件、unavailable / local mock 页面和维护规则。
- 2026-05-19：补充页面覆盖自检、iPhone 页面 chrome、iPad 空状态、macOS missing route fallback、macOS Settings scene 只读边界和共享承载组件。原因：复查 SwiftUI `View` / `Route` / `Sheet` / `Settings` 后，需要把非主 route 但会影响页面完整性的承载层写清楚。
- 2026-05-19：补充 iPad / macOS AI Provider 设置页一致性记录。原因：工作台内 AI Provider 详情页三端共享同一表单，iPad / macOS 只通过平台最大宽度控制大屏阅读栏，原生 macOS Settings scene 仍保持只读能力列表。
- 2026-05-19：补充 iPad 右侧学习面板 route-aware 边界。原因：设置、记忆、导入导出和语言空间 route 不应继续展示 entry 学习内容或请求预览。
- 2026-05-19：补充 iPad 顶部工具栏去重和右侧学习面板留白边界。原因：设置入口已由左侧和底部区域稳定承载，右侧面板容器和内容块都需要避免贴近设备边缘。
- 2026-05-20：补充 iPad / macOS 同步设置 UI 适配事实。原因：同步设置从 iPhone 单列 Local Mock 扩展为三端共享内容、iPad 设置焦点、macOS 工作台 embedded presentation 和原生 Settings scene 可进入详情；无语言空间时 Settings scene 只展示全局边界。
- 2026-05-20：补充宽屏 Onboarding 和 Mac 同步范围细节修正事实。原因：Onboarding 创建按钮在 iPad / Mac 宽屏下需要受主操作宽度约束，并在宽屏使用内容流内 CTA 避免顶部贴边和按钮贴底；同步范围附件项在 macOS 上需要显式 switch 表达，而不是默认 checkbox。
- 2026-05-20：统一三端设置能力项的导入导出命名。原因：iPad / macOS 已有导入导出工作台入口，但共享设置能力仍显示为“导出”，导致 iPhone 设置页语义缺项。影响范围：SettingsCapability、三端设置列表、设置详情本地化和页面清单。是否需要 ADR：否，未改变真实导入导出能力或核心数据策略。
- 2026-05-20：补充 iPad / macOS 语言空间管理入口事实。原因：iPhone 已落地的语言空间管理能力扩展到 iPad 工作台主区、macOS 工作台 Settings route 和原生 Settings scene，原 iPad / macOS summary-only 页面事实已过期。影响范围：iPad / macOS route、Settings scene、共享语言空间管理组件和 App Shell action 注入。是否需要 ADR：否，未改变语言空间核心模型或数据策略。
- 2026-05-22：修正 Onboarding 语言空间创建事实。原因：语言空间基础设施已通过 App Shell action、`LanguageSpaceRepository` 和 SQLite / GRDB 持久化落地，页面清单不应继续写成内存预览。影响范围：Root、Onboarding、启动恢复、语言空间管理和测试清单。是否需要 ADR：否，延续语言空间核心模型和 SQLite / GRDB 主存储路线。
- 2026-05-20：更新 AI Provider 设置页实现事实。原因：Provider 设置页已从 Local Mock 推进到 SQLite / GRDB metadata、Keychain secret 保存和本地 credential validation；真实外部 Provider 合成探测仍未接入。影响范围：iPhone / iPad / macOS 设置详情、AIProviderSettingsView、AppEnvironment、Data / AI package。是否需要 ADR：否，沿用 ADR-005。
- 2026-05-27：更新 AI Provider 已保存密钥查看事实。原因：已确认保留当前 API Key 字段 UI，加载配置页不自动解析 Keychain；已有 credential 时显示 `已保存到本机 Keychain` placeholder，点击小眼睛后才解析并回填到同一可编辑输入框。影响范围：AIProviderSettingsView、AIProviderDraftConfiguration、AIProviderSettingsActions、AIProviderSettingsComponents 和 AppEnvironment。是否需要 ADR：否，沿用 ADR-005。
- 2026-05-20：更新 AI Provider 已保存密钥回显事实。原因：用户再次打开配置页时需要看到并编辑已保存的本机 API Key；实现通过服务边界解析 Keychain，仅回填到短生命周期 UI draft，默认隐藏，不改变数据库、同步或真实 Provider 请求边界。影响范围：AIProviderSettingsView、AIProviderDraftConfiguration、AIProviderSettingsActions、AppEnvironment。是否需要 ADR：否，沿用 ADR-005；该事实已于 2026-05-27 收紧为用户点击小眼睛后才解析。
- 2026-05-21：更新 AI Provider 文本模型合成测试事实。原因：测试请求已从本地 credential validation 推进到 Provider 层固定合成文本探测，结果面板分项展示文本回复、JSON 输出、图片理解、语音生成和向量化；当时第一阶段只真实请求文本 endpoint，后三项只显示占位状态。影响范围：AIProviderSettingsView、AIProviderDraftConfiguration、AIProviderSettingsComponents、AIProviderSettingsActions、AppEnvironment、LangoTraceAI、LangoTraceData。是否需要 ADR：否，沿用 ADR-005；真网人工复核仍依赖可控测试 Provider / API Key。
- 2026-05-21：补充 iPad / macOS AI Provider 测试结果面板收口事实。原因：Provider 请求测试通过共享 `AIProviderSettingsView` 和 `AIProviderSettingsActions` 自然覆盖三端，但大屏结果内容需要明确最大宽度和测试锁定，避免把 iPhone compact detents 当成唯一 presentation。影响范围：AIProviderSettingsView、AIProviderSettingsProbeTests、iPad / macOS 设置详情。是否需要 ADR：否，未改变 Provider 请求、隐私或诊断边界。
- 2026-05-21：更新 AI Provider 图片理解合成测试事实。原因：图片理解不再只是占位；OpenAI Responses / OpenAI-compatible Chat 在用户显式启用图片输入时会发送内置白底蓝色正方形 PNG data URL，并严格校验 `blue square`。影响范围：AIProviderConfigurationProbeService、AIProviderProbeImageFixture、AIProviderSettingsView、Prompt Registry 和 AI Provider 隐私规范。是否需要 ADR：否，沿用 ADR-005；真实用户照片请求仍需另开方案。
- 2026-05-21：更新 AI Provider 图片理解能力边界事实。原因：OpenRouter / Custom OpenAI-compatible 的图片输入能力由具体模型决定，设置页不再用 Provider preset 静态布尔值禁用图片理解；UI 通过 Provider、adapter、endpoint purpose、模型策略和用户启用状态解析能力，`supportsImageInput` 只作为 endpoint 运行期防线和保存快照。影响范围：AIProviderSettingsModels、AIProviderDraftConfiguration、AIProviderSettingsView、AIProviderSettingsComponents、Core endpoint normalization 和 AI Provider 隐私规范。是否需要 ADR：否，沿用 ADR-005；动态模型列表和真实用户照片请求仍需另开方案。
- 2026-05-22：更新 AI Provider 语言支持合成测试首轮事实。原因：iOS Provider 设置页先新增当前语言空间上下文下的 `语言支持` probe，AI 层会让模型生成目标语言较长样例并用本机 JSON、长度、NaturalLanguage 和脚本规则校验；iPad / macOS 设置入口在后续人工审核节点后接入。影响范围：LanguageSpacePreview、AIProviderSettingsView、AIProviderDraftConfiguration、AIProviderSettingsActions、AppEnvironment、LangoTraceAI、Prompt Registry 和 AI Provider 隐私规范。是否需要 ADR：否，沿用 ADR-005；真实学习内容请求仍需单独请求预览方案。
- 2026-05-22：补充 iPad / macOS 语言支持入口事实。原因：iOS 人工审核后，iPad / macOS 工作台设置详情和 macOS 原生 Settings scene 通过共享 `SettingsCapabilityDetailView` 注入同一 target language code，不新增平台专属 AI Provider 表单。影响范围：SettingsCapabilityDetailView、AIProviderSettingsView、页面清单和 AI Provider 隐私规范。是否需要 ADR：否，沿用 ADR-005。
- 2026-05-23：更新一键学习材料生成的 iOS 优先落地状态。原因：iPhone 记录详情已从本地预览推进到真实 AI Provider 请求、GRDB LearningMaterial 保存、learning text 编辑和重新分析；iPad / macOS UI 接入按用户确认延后到 iOS 人工测试通过后。影响范围：EntryDetailView、LearningContentStore、LearningMaterialGenerationActions、AppEnvironment、LangoTraceData、LangoTraceAI 和页面清单。是否需要 ADR：否，沿用本地优先与三端共享业务逻辑决策。
- 2026-05-23：更新记录详情双文本卡片和原文编辑状态。原因：`EntryDetailView` 已改为母语原文 / 目标语言学习文本共享动态文本卡片，原文支持 sheet 编辑并通过 `source_entry_body_hash` 推导“基于旧记录”；三端通过共享详情组件提供显式重新生成。影响范围：iPhone 记录详情、iPad route detail、macOS Entry Detail、Data learning content schema 和 Store presentation state。是否需要 ADR：否，属于本地记录闭环和派生材料状态实现细化。
- 2026-05-23：补充外观设置与浅色 / 深色基础设施事实。原因：三端设置列表新增外观能力，App 层通过设备级 `AppearancePreference` 和 `preferredColorScheme` 即时切换浅色 / 深色，`LangoTraceDesign` 已接入 light / dark token；发布级视觉仍需截图或人工验收。影响范围：iPhone / iPad / macOS 设置入口、macOS Settings scene、共享设置详情和设计 token。是否需要 ADR：否，当前只实现系统外观偏好。
- 2026-05-23：更新逐句听读入口事实。原因：`LocalListeningPreviewView` 已删除，逐句 `听` 按钮不再打开解释型 sheet，只在 `SentencePairView` 内原位切换播放 / 暂停视觉状态；真实 TTS 生成、播放和跨句协调仍等待后续 TTS Provider 配置测试与 Speech 服务边界。影响范围：iPhone / iPad / macOS 共享 `SentencePairView`、页面清单和 UI 交互规范。是否需要 ADR：否，属于当前页面事实与组件交互约束更新。
- 2026-05-23：同步 AI Provider 设置页 TTS 配置测试实施事实。原因：语音生成分组已从占位推进到 voice / format / speed / instructions 配置、language code 级 voice profile、OpenAI / OpenRouter 固定低敏 TTS probe、endpoint metadata 结果和 Speech preview playback seam。影响范围：AIProviderSettingsView、AIProviderDraftConfiguration、AIProviderSettingsComponents、AIProviderSettingsActions、AppEnvironment、LangoTraceAI / Data / Speech 和页面清单。是否需要 ADR：否，沿用 ADR-005 和 TTS 规范 011。
- 2026-05-23：补充已保存 TTS voice profile 回填事实。原因：严格代码检查发现设置页重开时若只加载 endpoint 与 Keychain secret，voice、format、speed、instructions 会回到 provider 默认值并可能在下一次保存时覆盖用户配置；当前实现已通过 `AIProviderSettingsActions.loadTTSVoiceProfile` 按当前 language code 回填。影响范围：AIProviderSettingsView、AIProviderDraftConfiguration、AIProviderSettingsActions、AppEnvironment 和 UI 回归测试。是否需要 ADR：否，属于 011 规范下的状态同步修复。
- 2026-05-24：补充 AI Provider 系统级设置页标题事实。原因：Provider profile 与凭证是跨语言空间配置，设置详情内容区不再重复显示 `AI Provider` 和当前语言空间方向 / 等级；当前语言空间 target language code 只作为测试和 voice profile 上下文。影响范围：SettingsCapabilityDetailView、AIProviderSettingsView 和三端设置详情。是否需要 ADR：否，沿用 ADR-005。
- 2026-05-24：补充设置主列表状态角标事实。原因：`可用`、`本地优先`、`待配置` 等角标来自早期 UI 开发进度标注，不应作为用户设置主列表信息展示；三端设置列表和 macOS Settings scene 现在显式隐藏通用 `CapabilityStatusBadge`。影响范围：SettingsView、PadMainSections 设置列表、MacWorkspaceContentView settings section、LangoTraceSettingsSceneView 和后续 settings status projection。是否需要 ADR：否，属于 UI 信息层级和组件使用规则。
- 2026-05-23：优化 AI Provider 设置页 TTS 参数区样式事实。原因：iPhone 截图暴露音色和格式标题弱、语速 stepper 与设置页视觉不一致、“朗读指令”含义不清；当前实现改为“语音细节”子区、带说明的菜单行、统一语速控制和“朗读风格”文案。影响范围：AIProviderSettingsComponents、Localizable.xcstrings、TTSProviderSettingsTests 和页面清单。是否需要 ADR：否，属于 UI 表达修复。
- 2026-05-24：同步逐句 `听` 按钮真实 TTS 播放实施事实。原因：真实单句 TTS generation service、持久 playback service、local artifact resolver、playback coordinator、AppEnvironment 装配和 UI action contract 已落地，三端共享 `SentencePairView` 点击 `听` 时不再只是本地 mock。影响范围：SentencePairView、SentencePairActionControls、LearningContentStore、SentenceAudioPlaybackActions、AppEnvironment、LangoTraceAI / Data / Speech / Core 和 TTS 规范 011。是否需要 ADR：否，仍属于 ADR-005 下用户自带 Provider 与本地优先派生媒体资产路径。
- 2026-05-23：补充 iPad / macOS 一键学习材料生成接入事实。原因：iOS 人工测试通过后，iPad `workspaceOverview` / route detail 和 macOS Today / Entries detail 已复用共享 helper 注入 `LearningContentStore.generateLearningMaterial`、`cancelLearningMaterialGeneration`、`updateLearningText` 和 `analyzeCurrentLearningText`，三端记录详情均走同一真实生成 action seam。影响范围：PadMainSections、MacWorkspaceContentView、EntryDetailView、LearningContentStore、页面清单。是否需要 ADR：否，沿用三端共享业务逻辑与平台分别设计决策。
- 2026-05-26：更新练习模块实现事实。原因：练习 Tab 从任务类型 mock 改为记录卡片 -> 句子列表 -> 单句跟读录音完成闭环；`PracticeSessionView` 通过 `PracticeSessionViewModel` / `PracticeActions` 接入 GRDB practice session、句子快照、麦克风录音和本地 media artifact metadata，三端共享同一 route seed 与 action seam。影响范围：iPhone 练习 Tab、记录详情逐句练习入口、iPad / macOS practice route、AppEnvironment、LangoTraceCore / Data / Speech / UI。是否需要 ADR：否，沿用本地优先、用户显式触发和三端共享业务逻辑决策；录音同步 / 导出 / 可恢复备份另建架构备忘录。
- 2026-05-26：补齐单句练习页听示范和录音回放事实。原因：post-implementation 复审发现单句页虽能录音和完成，但缺少页内 TTS 示范播放和用户录音回放入口；当前已复用 `SentenceAudioPlaybackCoordinator` 处理示范播放，并通过 `PracticeActions.playRecording`、`GRDBPracticeRepository.readyRecordingArtifact` 和 `LocalMediaArtifactPlaybackSourceResolver` 接入 ready recording 回放。影响范围：PracticeSessionView、PracticeControlBar、PracticeSessionViewModel、LearningContentStore、PracticeActionsAssembly、三端 practice route 和 media artifact 实现地图。是否需要 ADR：否，属于既有练习闭环完成标准下的缺口修复。
- 2026-05-26：对齐 iPad / macOS 练习入口和滚动壳层事实。原因：iOS 重录流程确认后，iPad 学习面板和 macOS Practice section 有真实练习句子时应显示 ready，而不是沿用早期 mock 状态；macOS 练习列表和单句页不再套工作台外层滚动容器。影响范围：PadLearningPanelView、MacWorkspaceContentView、MacWorkspaceRoute、三端 practice route 和页面清单。是否需要 ADR：否，未改变练习数据、录音隐私或核心产品决策。
- 2026-05-26：移除记录详情中的 practice candidate 信息卡事实。原因：iPhone 练习页已收敛为记录卡片 -> 句子列表 -> 单句跟读录音，iPad / macOS 复用共享 `EntryDetailView` 时不应继续在记录详情底部显示“进入练习”、反向翻译或跟读等信息性候选卡。影响范围：EntryDetailView、iPad / macOS 记录详情和页面清单。是否需要 ADR：否，属于三端 UI 信息层级对齐，不改变练习数据模型或录音隐私边界。

- 2026-05-27：同步 AI Provider 设置页向量化配置测试实施事实。原因：向量模型 endpoint 已从占位推进到 OpenAI / OpenRouter / Custom OpenAI-compatible 第一阶段固定低敏 embeddings probe，结果携带 embedding endpoint metadata 和 configuration fingerprint，saved profile 结果写 endpoint-scoped validation outcome，不污染文本 profile 全局摘要。影响范围：AIProviderSettingsView、AIProviderDraftConfiguration、AppEnvironment、LangoTraceAI、LangoTraceData、Prompt Registry 和测试文档。是否需要 ADR：否，沿用 ADR-005。
- 2026-06-01：新增 Reading 一级页面事实。原因：阅读纵向切片已将 iPhone Tab、iPad route、macOS section 接入真实 `ReadingLibraryStore` / GRDB reading repository / Markdown renderer / selection explanation / reading sentence TTS。影响范围：PhoneMainView、PadMainSections、MacWorkspaceContentView、AppEnvironment、LangoTraceCore / Data / AI / UI 和 Reading spec。是否需要 ADR：否，沿用本地优先、语言空间和用户显式触发边界。
- 2026-06-11：在「本文档不替代」列表加入 `prototypes/` 目标设计基准定位。原因：多端原型已重建并覆盖待开发页面的目标设计，需要在页面事实源侧明确原型表达目标设计而非实现事实，避免原型「目标设计」页面被误读为已实现。影响范围：本文档与 `prototypes/`、`docs/spec/003-ui-design-system.md` 的权威关系标注。是否需要 ADR：否。
- 2026-06-17：将「照片写作预览」从 Local Mock 更新为「照片写作」Implemented。原因：E2 任务（feature-entry-photo-attachment-and-photo-writing）已完成照片选取、EXIF GPS 剥离、缩略图生成、`entry_photo_attachments` 持久化、`EntryCard` 缩略图展示和 `EntryDetailView` 全宽照片展示；`PhonePhotoWritingPreviewView.swift` 和 `createMockPhotoWritingEntry()` 已删除，替换为 `PhotoWritingView.swift`、`PhotoWritingActions`、`PhotoImportPipeline`。影响范围：iPhone 记录 Hero 入口、EntryCard、EntryDetailView、AppEnvironment `photoDisplayActions` 装配；`PhoneSheet.photoWritingPreview` → `photoWriting`；`PhotoDisplayActions` 环境值通过三端根视图注入。是否需要 ADR：否，沿用本地优先和用户主资产边界；照片字节不发送 AI Provider。
- 2026-06-17：同步 R1 阅读体验收口事实。原因：R1 四个 Phase 已全部落地（2026-06-17）。主要变化：(1) 资料库文档行新增 `字数·导入日期·进度` 三段元信息和收藏星标；(2) 筛选区新增 `全部 / 收藏` chips；(3) iPad inspector 可通过顶部按钮折叠，折叠后降级为画布底部 compact 面板；(4) `ExplanationLanguageModePicker` 三档切换控件嵌入 compact 面板与 iPad / macOS inspector；(5) `reading_documents` 新增进度 / 收藏列（v22 migration），`reading_structure_blocks` / `reading_sentences` 新增 `content_revision` 列（v21 migration）；(6) `rebuildStructure` 改用真实分句。影响范围：iPhone 阅读 Tab、iPad 阅读资料库 route、macOS Reading section。是否需要 ADR：否，沿用本地优先、用户显式触发和语言空间边界；进度 / 收藏不参与同步。
- 2026-06-18：同步 E4 听写练习落地事实。原因：E4 落地听写本机对照闭环——`PracticeSessionView` 按 exercise type 分发到新增 `PracticeDictationSessionView`（跟读会话同时抽出为 `PracticeShadowingSessionView`），`PracticeDictationDiff`（Core 纯函数）做声学/词汇层对照，attempt 写入新增 `practice_text_attempts`（v23），App 装配注册 `shadowing / dictation` 两段并把 `createOrRestoreShadowingSession` 泛化为 `createOrRestoreSession`；句子列表 mode 分段控制随之出现，听写“已练”由文本 attempt 派生。影响范围：练习 Tab 句子列表、练习会话（跟读 / 听写）、记录详情逐句练习入口、三端 practice route、`PracticeActions` 装配、LangoTraceCore / Data / UI。是否需要 ADR：否，沿用本地优先、用户显式触发、不做机械评分 / ASR / 外发决策。
- 2026-06-18：同步 E5 回译练习（Slice 1，纯本地）落地事实。原因：E5 Slice 1 落地回译本地参考对照闭环——`PracticeSessionView` 拆出独立 `.backtranslation` case 路由到新增 `PracticeBacktranslationSessionView`，三态为题面（母语原意）→ 作答 → 对照参考；参考富字段经新增只读 seam `LearningContentRepository.sentenceAnalysis(materialID:sentenceIndex:)` 在 reveal 时懒加载并以 snapshot 兜底，作答写入 `practice_text_attempts`（exercise_type=backtranslation，diff 列由 `recordTextAttempt` repository 不变量恒 NULL、listen_count 恒 0）；App 装配注册第三段 `.backtranslation` 并接 `submitBacktranslationAttempt` / `fetchSentenceAnalysis`，句子列表出现 跟读 / 听写 / 回译 三段控制，回译“已练”由文本 attempt 派生。Slice 2（可选 AI 点评）双重门禁待 E6 + 单独隐私授权，未实现。影响范围：练习 Tab 句子列表、练习会话（回译）、三端 practice route、`PracticeActions` 装配、LangoTraceData（新增 sentenceAnalysis seam + diff-NULL 不变量）/ UI。是否需要 ADR：否，沿用本地优先、用户显式触发、回译不判对错、参考只来自既有分析不发新请求。
- 2026-06-17：同步 E3 练习方式路由基础事实。原因：`PracticeExerciseType` 已扩展为 shadowing / dictation / backtranslation，v18 migration 扩展了 `practice_sessions.exercise_type` CHECK 约束，`PracticeSessionRouteSeed` / snapshot / 句间导航贯通 exercise type，句子列表新增 mode availability、已练状态和续练 CTA；当前 App 只注册 `.shadowing`，未暴露听写 / 回译假入口。影响范围：练习 Tab 句子列表、记录详情逐句练习入口、三端 practice route、AppEnvironment `PracticeActions` 装配、LangoTraceCore / Data / UI。是否需要 ADR：否，沿用三端共享业务逻辑和本地优先练习录音边界。
- 2026-06-18：同步 E6 AI 请求预览 + 请求日志基础落地事实。原因：`RequestPreviewCard` 从占位 Local Mock 推进到真实「将发送内容」投影渲染（provider preset / model / 长度分桶 / 含的内容类别 + 不发送清单；mock 仍显示本地草稿，未配置 Provider 显式离线态），并新增内容无正文、真实数据驱动的 `AIRequestLogListView`。主要事实：(1) iPad 学习面板与 macOS Inspector 预览卡接入投影，并在其下新增请求日志列表；(2) 请求日志列表同时挂在「设置 → AI Provider」详情（跨平台、含 iPhone 可达），iPhone 记录详情仍**不**出现持久预览卡（§8 红线保持）；(3) 日志行只展示时间 / 能力 / 服务方 / 状态 / 失败分类，无任何请求内容；(4) `v24_create_ai_request_logs` migration，App-Shell `AIRequestLogRecorder` 在真实 generate/analyze/explain 三态写入本地日志。影响范围：iPad 右侧学习面板、macOS Inspector entryDetail、设置 AI Provider 详情、`RequestPreviewCard`、新增 `AIRequestPreviewPresentation` / `AIRequestLogListView`、LangoTraceCore / Data / AI / UI、App Shell。是否需要 ADR：否，沿用本地优先与用户显式触发；请求日志为本地诊断性数据不同步不导出。
- 2026-06-18：同步 E5 Slice 2 回译可选 AI 点评落地事实。原因：回译会话页在「对照参考」展开后新增显式「请 AI 点评 / Ask AI to review」入口——按钮上方 footnote 提前披露发送范围（你的作答、母语原意、参考表达）与时机（仅点击时发送），状态机 idle→sending（含取消）→reviewed（致意 + 观察 + 建议 + 语体，无判定/评分）/failed（精确分类，不伪装网络失败）；发送经 E6 请求预览投影（capability `practiceBacktranslationReview`）并写 `ai_request_logs` 非敏感日志，点评正文不持久化。运行期保留显式触发（核心决策 10），绝不自动外发。影响范围：练习回译会话页（`PracticeBacktranslationSessionView`）、`PracticeActions.reviewBacktranslation`、App Shell `PracticeActionsAssembly`、LangoTraceAI（`PracticeBacktranslationReviewService`）/Core/UI。是否需要 ADR：否，沿用本地优先与用户显式触发。
- 2026-06-18：同步 E9 本地 FTS 全文搜索落地事实。原因：macOS 搜索从 `.unavailable("search")` 占位推进为真实命令面板（复用既有搜索命令 ⌘F，工具栏放大镜与命令均唤起 `SearchPaletteView` 浮层），iPad 顶部搜索从 `PadSheet.unavailableSearch`（`UnavailableCapabilityView`）替换为同一 `SearchPaletteView` sheet。主要事实：(1) 共享 `SearchPaletteView` + `SearchPaletteStore`（防抖、取消防竞态、跨组键盘选择、零态、打开时 rebuild 索引）；(2) 分组结果（记录/阅读/记忆，记忆在 E7 前零态）、底部「范围：当前语言空间」披露；(3) 命中：记录→记录详情、阅读→阅读区；(4) 全程本地、不发网络，FTS 索引为可重建派生数据（不同步/导出非必需）；(5) iPhone 本阶段不提供全局搜索入口（原型无该页，记录/阅读列表各有浏览路径）。影响范围：macOS `MacMainView` 搜索命令/工具栏/overlay、iPad `PadMainView`/`PadMainModels` 搜索 sheet、新增 `SearchPaletteView`/`SearchPaletteStore`、LangoTraceCore（SearchModels）/Data（v25 FTS5 + repository）、App Shell 装配。是否需要 ADR：否，FTS 沿用派生数据既有边界。
- 2026-06-18：同步 E7 记忆沉淀基础落地事实。原因：新增 `memory_items` 主数据沉淀表（v26）+ 显式「加入记忆」动作 + 三端只读沉淀列表 + 真实 `settled` 时间线筛选。主要事实：(1) iPhone 记忆 Tab 顶部新增「已沉淀记忆」只读列表 + 候选行的 加入记忆/已加入 显式动作；(2) iPad `.memory` 路由与 macOS 记忆区同样展示只读沉淀列表；(3) 时间线「已沉淀」筛选 chip 在 iPhone/iPad 取消隐藏并由 `depositedEntryIDs` 真实驱动（含 iPad 侧栏计数）；(4) 沉淀为本地主数据快照，候选重生成不影响；同步/导出留 E10/E11。影响范围：iPhone `MemoryView` + `PhoneRecordWorkspaceView` 时间线、iPad `PadMainSections`/`PadMainView`、macOS `MacWorkspaceContentView`、新增 `MemoryDepositActions` seam、LangoTraceCore（`DepositedMemoryItem`）/Data（v26 + `GRDBMemoryItemRepository`）、App Shell。是否需要 ADR：否，沿用本地优先 + ADR-004 空间归属。
- 2026-06-18：同步 E8 记忆复习队列落地事实。原因：记忆面新增低压力复习闭环。主要事实：(1) iPhone 记忆 Tab 顶部新增统计条（本周沉淀/待复习/已掌握）+「开始复习」按钮 → 复习会话 sheet；(2) 共享 `MemoryReviewSessionView`（目标文本→看释义→记得/还要再看→标记已掌握→完成总结，小批量默认 10，中途退出安全）；(3) 基于 E7 review 列的本地固定间隔调度（Core 纯 `MemoryReviewScheduler`），无新 migration、不发网络。影响范围：iPhone `MemoryView`、新增 `MemoryReviewActions`/`MemoryReviewSessionView`/`MemoryStatisticsBar`、LangoTraceCore（`MemoryReviewScheduler`）/Data（repository review 方法）、App Shell。后续：iPad/macOS 复习入口（统计条/会话已可复用同 seam 与 view）随后补。是否需要 ADR：否，纯本地复习本地数据。
- 2026-06-19：同步记录详情信息架构重构 + 双语沉浸阅读页 + 手动重生成落地事实。原因：记录详情两段全量文本导致页面过长且英文重复，逐句学习区被压到首屏之下。主要事实：(1) 三端共享 `EntryDetailView` 的中文记录与英文表达卡改为按行数折叠（`EntrySourceCollapsePresentation`，`lineLimit` + 展开/收起，尊重 Dynamic Type，不固定像素高度）；(2) 英文表达卡的「重新翻译（按中文记录）/ 重新讲解（当前英文）」收敛为常驻「重新生成」`Menu` + 覆盖 `confirmationDialog`（`LearningMaterialActionAvailability`，不再按 stale 显隐、仅运行中 / blocked 禁用，符合核心决策 #10 显式触发）；(3) 新增三端 `bilingualReading(entryID)` 路由与双语沉浸阅读页（`EntryReadingViews`/`EntryReadingRouting`），只带 entryID、以实时 rendering 为源（不快照），点句显隐译文/讲解 + `@AppStorage` 字号 + 连续 TTS（`SentenceSequencePlayback` Core 状态机 + store 编排，store 持续播意图、仅自然播完推进，当前句高亮）；(4) 离开页只停播放不取消生成中请求（已显式触发，#10 不破）。影响范围：`PhoneMainSupportingViews`/`EntryDetailPresentation`/`EntryReadingViews`/`EntryReadingRouting`、三端 route 枚举与 destination switch、`LearningContentStore`、`SentenceSequencePlayback`（Core）、`Localizable.xcstrings`、对应单测。是否需要 ADR：否，沿用本地优先、三端共享业务逻辑与用户显式触发；详见 `docs/plans/done/2026-06-19-feature-entry-detail-reading-restructure.md`。
- 2026-06-20：同步 iPhone 四 Tab 统一导航栏上下文条落地事实。原因：记录页顶部大块留白未利用，且设置齿轮逐页手搓导致阅读 Tab 缺失，三 Tab 不一致。主要事实：(1) 新增 `PhoneRootContextToolbar.swift`——`phoneRootContextToolbar` 共享修饰符把语言胶囊（leading）+ 设置齿轮（trailing）上移进 inline 导航栏，四 Tab（含阅读）共用，结构上不再漏齿轮；(2) 方向 B「统一描边控件」：胶囊与齿轮同一 hairline 控件家族（同高、同描边、无阴影、去地球图标），居中标题加重为锚点，触控热区 ≥44pt；(3) `PhonePage` 以 `showsContextHeader` 门控（`SettingsView` 仍 `false`，不注入胶囊/齿轮），正文保留 20pt 顶部呼吸、回收大标题带，移除正文内 `PhoneContextHeader` 并退役该文件；(4) `ReadingLibraryView` 新增 `onLanguageSpaceAction`/`onSettingsAction` 可选闭包（默认 nil，iPad/macOS 调用点不变），阅读 Tab 接线切换 sheet 与 `.settingsList`；(5) `PhoneRootContextChrome`（presentation 模型）+ `PhoneRootTab.providesRootContextToolbar` 提供可测守卫。验证：`swift test --package-path Packages/LangoTraceUI` 556 passed、`swiftformat --lint` 0、`swiftlint` 0 serious；模拟器人工验收待执行。影响范围：iPhone 记录/阅读/练习/记忆 Tab chrome、`PhoneMainSections`/`PhoneMainView`/`ReadingViews`、新增 `PhoneRootContextToolbar`、退役 `PhoneContextHeader`、LangoTraceUI 测试。是否需要 ADR：否，对齐既有导航 spec（spec/002 §24/§54-55、spec/003 §143），未反转核心决策；详见 `docs/plans/done/2026-06-20-refactor-phone-root-context-toolbar.md`。
- 2026-06-20：导航栏控件克制化迭代（去边框 + 紧凑文案，方向 A）。原因：首版「胶囊描边 + 齿轮描边」双容器叠在内容卡描边上显繁杂、左右失衡。主要事实：(1) 语言 chip 与设置齿轮均去描边 / 填充，改裸控件，平衡靠同样低对比而非等宽；(2) 语言显示收敛为 spec/002 §54 / §157 的 `英语 · B1`（目标语 + 等级），完整 `中文 -> 英语 · B1` 仅作 VoiceOver value；(3) `PhoneRootContextChrome` 新增 `compactContext` 纯函数投影（单测覆盖），`PhoneLanguageSpaceChip` 改 `label + accessibilityContext` + `chevron.down` 指示 + `fixedSize` 防截断，移除 `PhoneRootControlSurface` 描边修饰符；(4) 否决语种代码（`cn` 是国家码非语种码、冷感、违 spec）。验证：UI 557 passed、构建成功；实机记录 tab 截图待人工走完 onboarding 补（重装清数据 + 本机无 headless 点按工具）。影响范围：`PhoneRootContextToolbar`、LangoTraceUI 测试。是否需要 ADR：否。
- 2026-06-20：顶部语言控件收敛为仅目标语言（第四轮）。原因：去边框后用户反馈左右仍不够协调，并提出只保留语种 + 加描边收成小 tag。评审（原型第四轮三方向对比）采纳「去 B1 但不加描边」（V3）：缩短为 `英语` 后左侧体量自然靠近齿轮，协调感不靠描边即可达成，延续第二轮「零描边最克制」共识；否决加描边（chip 加边 / 齿轮裸露语汇不一致；两边都加边又回到双描边）。主要事实：`PhoneRootContextChrome.compactContext` 由 `目标语 · 等级` 改为仅 `targetLanguage`；完整 `中文 -> 英语 · B1` 仍为 VoiceOver value；单测改为断言 `compactContext == targetLanguage` 且不含等级；spec/002 §54 / §157 示例措辞同步。验证：UI 557 passed、Han 守卫通过、`swiftformat --lint` 0。影响范围：`PhoneRootContextChrome`、UI 测试、spec/002、本清单。是否需要 ADR：否，未改变语言空间核心模型或 A1-C2 数据模型。
- 2026-06-24：同步照片写作 AI 看图辅助写作落地事实。原因：照片写作页此前对照片做无条件「永不发送 AI」承诺，与即将开发的看图辅助写作冲突。主要事实：(1) 照片写作页新增显式「让 AI 看图帮我写」动作（模式：写作提示 / 母语草稿），发送前确认披露「将发送照片 + 备注」；(2) 发送的图片经新增 `AIImageSanitizer` 降采样到 max edge 1024 + 再次剥离 EXIF/GPS 的脱敏产物，**不复用 256px 缩略图、不发原图、不发原始相册字节**（脱敏单一入口）；(3) 新增 `AIRequestCapability.photoWritingAssist` 与其请求预览投影，是唯一在 included 中含 `.photoAttachments` 的能力，其余能力的 `alwaysExcludedContent` 不变、仍排除照片；(4) adapter 新增 `structuredImagePromptBody`（图片 + json_schema），仅 OpenAI 兼容 Chat / Responses 支持，mimo/Anthropic/Gemini 结构性不支持；门控顺序 `supportsImageInput → imageInputEnabled → adapter allowlist`，与配置图片 probe 共用 `AIProviderImageSupport` 单一 allowlist；(5) 结果非破坏性（母语草稿可「采用」追加、写作提示只读），经 `ai_request_logs`（capability=`photoWritingAssist`）记非敏感日志，正文不持久化；(6) 隐私文案改为分动作准确表述。专用 `photo_writing_assist_operations` 摘要表 v1 延后（`ai_request_logs` 已覆盖逐请求透明度）。影响范围：照片写作页（`PhotoWritingView`/`PhotoWritingAssistViewModel`/`PhotoWritingAssistResultPanel`）、`PhotoWritingActions`、App Shell `PhotoWritingActionsAssembly`、LangoTraceCore（`PhotoWritingAssist`/capability）/Data（`AIImageSanitizer`）/AI（`PhotoWritingAssistService`/`structuredImagePromptBody`/`AIProviderImageSupport`）/UI、`AIRequestLogListView`、`Localizable.xcstrings`。是否需要 ADR：否，符合核心决策 #10（敏感内容仅显式触发时发送），沿用 ADR-005；详见 `docs/plans/active/2026-06-24-feature-photo-writing-ai-assist.md`。
- 2026-06-24：照片写作页移除底部常驻隐私提示横幅以保持简洁。原因：用户反馈页面底部 tip 影响简洁度。主要事实：(1) 删除 `PhotoWritingView` 的 `privacyNotice` 页内 banner 视图与 `photoWriting.privacy.notice` 文案 key（en + zh-Hans）；(2) 隐私披露不降级——发送前确认弹窗仍完整披露「将发送照片 + 备注，不发送其它内容」，且与发送动作原子绑定，成为唯一披露界面；(3) 本地化守卫从「常驻 banner 文案」迁移到「发送前确认弹窗披露发送范围」，并新增断言 banner key 已删除（`PhotoWritingAssistLocalizationTests` 3 项通过）。影响范围：`PhotoWritingView`、`Localizable.xcstrings`、`PhotoWritingAssistLocalizationTests`、spec/005 §4.8、本清单。是否需要 ADR：否，隐私底线不变，沿用核心决策 #10 与 ADR-005。

# FABLE 任务简报 — LangoTrace 深度审查与推进

> 这是一份写给 **Claude Fable 5** 的自主 goal 任务简报，不是分步脚本。
> Fable 5 的正确指令方式是「给目标和边界，不给流程」——见
> [`docs/reference/fable-5.md`](docs/reference/fable-5.md)。因此本文只声明目标、授权、
> 不可逾越的边界和「完成」的样子，路径由你自己规划。信任你的判断，守住边界。

---

## 0. 你在自主运行

你正在 **自主运行**，用户没有实时盯着。这是一次长时程 agentic 任务，可能持续数小时到数天。

- **不要中断、不要停下等确认。** 遇到问题时，基于「产品北极星、核心决策、既有文档」自行深入思考并决策，然后继续。
- 对**符合本简报意图的可逆动作**，直接做，不要问。**当你已有足够信息可以行动时，就行动。**
- 实现前按 `CLAUDE.md` §5「按任务类型读取文档」的阅读路径，读相关 spec / ADR / architecture / 页面清单，避免导航、UI、数据、AI 请求路径发散——这是本仓库的既定纪律，不是可选项。
- **结束每一轮回合前**：如果你最后一段话是一个计划、一个问题、或一句「我接下来会…」的承诺，那就现在用工具把它做掉，再结束。不要以待办清单收尾。
- 你的**上下文余量充足**。看到 token 倒计时也不要提议总结或交接——继续工作。
- 长会话里用 `.claude` memory 和 active plan 记录教训：一事一文件，顶部一行摘要，更新而非复制。

**任务日志（贯穿全程）。** 根目录已备好 [`FABLE-WORKLOG.md`](FABLE-WORKLOG.md)。**开工第一件事就是接手它，并在整个运行过程中持续追加更新**：记录操作日志（做了什么、动了哪些文件）、遇到的问题、**决策依据**（为什么这么选、参考了哪条决策/文档）、CI run 编号与结论、下一步。按时间倒序或正序追加均可，但要让「运行结束后第一次来看的用户」能顺着它复原你这次做了什么、为什么。这份 worklog 随代码一起分批提交推送，是你对用户的主要交代面。

## 1. 任务意图（为什么）

LangoTrace（语迹）是一款「用生活记录学习语言」的本地优先三端语言学习 App，目前处于早期开发阶段：已有启动路由、语言空间持久化、三端骨架、真实 learning-content / AI Provider / TTS / 练习录音路径，但完整时间线、附件、评分/听写/回译、Embedding、OCR/语音识别、同步引擎、StoreKit、发布材料等**大量任务尚未完成**。

这次运行的目的：**在 `dev` 分支上，对整个项目做一次深入的审查与优化，并实质推进这些未竟任务**，让产品朝北极星更进一步，同时让文档控制面与代码保持一致。

## 2. 目标（做什么）

在 `dev` 分支上，端到端地：

1. **深入审查**当前实现、架构边界、文档一致性，找出问题、债务、不一致和风险。
2. **优化**已有实现（正确性、长期架构清晰度、三端体验、测试可维护性——按这个优先级）。
3. **完善并开发未竟任务**。范围由你**完全自主判断**：不预设优先级约束，你可以从 `CLAUDE.md`「尚未完成」清单中自行选择推进哪些、推进到什么程度，包括同步、StoreKit 这类重型能力。用产品价值和工程成熟度判断先后，克制而扎实地推进，而不是浅尝辄止铺一堆半成品。
4. 每推进一块，就通过 CI 验证、分批提交并推送远程 `dev`。

你对 `dev` 分支拥有**最高的判断与编辑权限**：可以推翻早期 mock、重构模块边界、重写落后的实现与文档——这正是早期阶段该做的（见 `CLAUDE.md` §1.1/§1.2）。**重要重做仍需先写 active plan**，但在本次授权下你**可以自批该 plan**（见 §4）。

## 3. 不可逾越的边界（守住这些）

授权不等于放松边界。以下是事实，不是脚手架，请始终守住：

- **只在 `dev` 上工作。`main` 是受保护发布主干，禁止本地 merge/push 到 `main`，本次运行也不开向 `main` 的 PR**——把 `main` 留给用户。（`CLAUDE.md` 核心决策 18）
- **核心产品与架构决策不可轻易破坏**（`CLAUDE.md` §4）。要改动其中任何一条，必须新增或更新对应 ADR，并在 plan 里讲清理由；不能把代码偏差包装成新决策。
- **隐私与本地优先边界不可破**：凭证/API Key/密钥进 Keychain 或等价安全存储、默认不同步；照片/日记/音频等敏感内容只有在用户显式触发对应 AI 能力时才发给 Provider；向量索引是可重建派生数据、默认不同步。
- **文档是控制面，不是事后说明。** 发现规范过期或有更优设计时，同步更新对应 spec / architecture / ADR / review / plan，别只留在提交信息里。发现文档体系结构性问题时按 `docs/review/` 机制处理；**删除历史记录、改写核心决策、改变文档权威关系**属于需谨慎的动作，优先降权/归档/补充当前事实源，而非直接删（`CLAUDE.md` §1.3）。
- **TDD 默认**：行为可自动化验证时，先在所属 Package 的 `Tests` 写/改一个会失败的用例，再做最小实现，最后验证。不写测试要在 plan 里说明原因与剩余风险。
- **依赖与工具链钉版不可隐式漂移**：GRDB 在所有 `Package.resolved` 保持 **7.10.0**（本机 Xcode 无法编译 7.11.0）；CI 的 SwiftFormat 钉 **0.62.1**。要升级必须显式、连带做重排/适配，绝不隐式 bump。
- **一切工作产物落在仓库内**（原型、脚本、证据、plan），不落到 `~/Desktop`、`/tmp` 等仓库外；无保留价值的中间产物用完即删、不提交。

## 4. Active Plan：写，但自批

`CLAUDE.md` 核心决策 16 要求实现前先建 `docs/plans/active/YYYY-MM-DD-<type>-<short-topic>.md` 并经用户确认。本次自主运行下：

- 每项实质任务**仍要写 active plan**（说明为什么、替代方案、改/删什么、TDD 落点、聚焦与完整验证命令、剩余风险），并按 `docs/plans/plan-review-protocol.md` 做**严格自审核**，把确认的问题、修订与 TDD 落点写回 plan。
- 但**由你自批**——写完 + 自审核通过即可进入实现，无需停下等用户确认。
- 完成后把 plan 移入 `docs/plans/done/`。
- 高风险/阶段性完成后按 `docs/review/` 做文档影响检查。

## 5. 运行事实（你无法从代码推断，但必须照做）

**测试环境。** 你在 Linux 上运行，**无法在本地构建/测试这个 Swift/Apple 工程**。所有构建与测试**只能靠 GitHub Actions**（`.github/workflows/ci.yml`，job 名 `Build & Test`，`macos-15`，40 分钟超时）。本机 `scripts/verify.sh` 与 `swift test` 在此环境不可用——不要假装跑过它们。

**触发 CI（关键）。** CI 对 `dev` 的**直接 push 只有当提交信息包含 `[ci]` 时才运行** `Build & Test`（`if: contains(head_commit.message, '[ci]')`）。因此：

- 日常小步提交**不必**带 `[ci]`，用于节省 macOS 额度、积累工作。
- 当你需要验证一个有意义的检查点时，让该次 push 的 **HEAD 提交信息带 `[ci]`**，CI 才会跑。
- 把 `[ci]` 当作你**主动的「现在验证」开关**，在每个可验证的里程碑用它。

**观察 CI（无 `gh` 凭证）。** 本 Linux 环境**没有 `gh` 凭证**，不要用 `gh run`。仓库在本次运行期间是 **public**，因此用**未认证的 GitHub REST API**（`curl`）读取结果即可：

- 最近运行：`curl -s "https://api.github.com/repos/zibuyu2015831/LangoTrace/actions/runs?branch=dev&per_page=5"`，看 `status`（queued/in_progress/completed）与 `conclusion`（success/failure）。
- 定位失败步骤：取 run 的 `id`，`curl -s ".../actions/runs/<id>/jobs"`，逐 step 看 `conclusion`——失败步骤名通常就能定位问题（如 `SwiftLint` / `SwiftFormat` / `Test LangoTraceUI` / 某个 `Build`）。
- 未认证 API 限速 **60 次/小时/IP**：**克制轮询**（一次 CI 通常十几到几十分钟，运行中每 2–3 分钟查一次足矣，别更密），命中限速就退避，不要空转。
- 红了就基于失败步骤深入诊断、修复、再带 `[ci]` 推一次；**别在红着的 CI 上继续堆新功能**。

**仓库可见性。** 不归你管：用户已在运行前把仓库设为 public，运行结束后由用户手动设回 private。**你不要尝试切换可见性**（也没有 `gh` 可用）。

**提交与推送。** `origin` = `git@github.com:zibuyu2015831/LangoTrace.git`（SSH，push 可用）。**分批提交并持续推送远程 `dev`**：每个逻辑完整的小步一个提交，信息清晰、遵循仓库既有中文提交信息风格（如 `feat(ui): …` / `chore(ci): …`）。别攒一个巨型提交。

**子代理与轻量模型（放手用）。** Fable 5 能可靠地派发并维持并行子代理，且**新上下文的 verifier 子代理优于自我批判**。

- 把**高吞吐、机械性**的侧活（跨文件搜集、批量小改的落点勘查、文档链接核对、样板测试骨架、日志/清单整理）派给**轻量模型子代理（Haiku）以节省额度**。
- 把**需要判断**的工作（架构决策、方案自审核、隐私边界、跨端设计、红 CI 的根因诊断）留在你自己或同级子代理上。
- 用**独立新上下文的 verifier 子代理**复核你的重要结论，别只靠自我复核。
- 主上下文保持精简。

## 6. 用证据支撑进展

在报告任何进展前，**对照本次会话的工具结果逐条核对每个论断**：说「测试通过」就要有对应的 CI `conclusion: success`；测试失败就如实带上失败步骤说明；跳过的步骤就说跳过了。**不要臆造状态**。CI 是唯一权威的构建/测试事实源。

## 7. 「完成」的样子

这是一次长时程推进，不追求一次做完所有未竟功能，但每一段可交付的成果应满足：

- 选定推进的任务有 active plan（已自批、已自审核、已归档到 `done/`），且未破坏 §3 的任何边界。
- 相关行为有先失败、后通过的单元测试覆盖（除非在 plan 里说明了为何不能自动化）。
- 对应 spec / architecture / ADR / review / 页面清单 已随代码更新，文档控制面与代码一致。
- 该检查点已带 `[ci]` 推送 `dev` 且 **CI `Build & Test` 为 success**（红则已修复或已如实记录阻塞与原因）。
- 变更以清晰的分批提交推送到远程 `dev`。
- [`FABLE-WORKLOG.md`](FABLE-WORKLOG.md) 已随进展持续更新，能让用户复原本次做了什么、为什么。

收尾消息面向「运行结束后第一次来看的用户」：**结论先行、平实句子、不用箭头链或自造缩写**，讲清做了什么、CI 现状、下一步建议。
